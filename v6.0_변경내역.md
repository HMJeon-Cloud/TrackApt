### v6.0 — 밝은 토스 스타일 개편 (2026.09)

PROJECT.md 최신 구간 맨 위에 붙여 넣으세요.

**화면 구조**
- 첫 화면을 홈으로 바꿨습니다. 버튼은 **아파트 추천받기**와 **시세 둘러보기** 두 개이고, 그 아래에 "많이 보는 화면" 칩이 있습니다.
- 메뉴는 5개로 줄였습니다: 홈 · 추천 · 둘러보기 · 내 후보 · 더보기. 휴대폰에서는 아래 탭바, 넓은 화면에서는 위쪽 밑줄 탭으로 나옵니다.
- 둘러보기와 더보기는 목록 화면입니다. 둘러보기는 시세 보기 · 지역 고르기 · 아파트 고르기 세 묶음으로 나뉩니다. 같은 묶음의 화면은 위쪽 알약 버튼으로 오가고, "‹ 묶음 이름"을 누르면 목록으로 돌아갑니다.
- 화면 id는 바꾸지 않았습니다. 그래서 `#tiers` 같은 옛 링크도 그대로 열립니다(v5.51 initialTab 유지). 주소에 `#`이 없으면 홈에서 시작합니다.

**설명은 팝업으로**
- 카드 설명문(p.desc), 안내 상자(.note·.callout), 접는 설명(details), 용어 풀이(.dict)는 제목 옆 **?** 로 옮겼습니다. 화면에는 첫 문장이 짧을 때만 한 줄 부제로 남깁니다.
- 계산 결과가 들어가는 칸(id가 있는 칸과 그 안쪽)은 옮기지 않습니다.
- 설명만 있던 카드(로드맵의 "왜 이렇게 보나" 등)는 한 줄 버튼 카드가 됩니다.
- 팝업은 노드를 옮겼다가 닫을 때 제자리로 돌려놓습니다. 복제하지 않습니다.
- 추천받기 단계 안내와 결과 하단 안내는 "ⓘ 도움말" 버튼으로 바꿨습니다. 총평 표는 접힌 상태로 둡니다.

**긴 화면 나누기** (구역 버튼, 카드는 그대로 두고 보이기만 전환)
- 시장 흐름, 입주 물량, 지역 채점표, 최종 검토, 재미로 보기, 지역 순위, 전세 끼고 사기, 갈아타기

**색·컴포넌트**
- 바탕 #f2f4f6, 흰 카드(모서리 20px), 강조색 파랑 #3182f6, Pretendard 글꼴
- 등락은 한국식입니다: 상승 빨강 #f04452, 하락 파랑
- 경고는 주황 #f08c00, "합격/가능"은 초록 #03a15b
- 버튼, 세그먼트, 입력칸, 표, 시트, 진행창, 지역 선택기 모양을 통일했습니다.
- 차트 격자, 레이더, 밴드 막대, 지도 핀, 이미지 저장 배경을 밝은 바탕에 맞췄습니다. 색 이름이 들어간 안내문("청록 막대" 등)도 새 색에 맞게 고쳤습니다.
- 용어 도움 ? 버튼은 오른쪽 아래 떠 있던 자리에서 머리글 안으로 옮겼습니다.

**바꾸지 않은 것**
- 계산 로직, 데이터 처리, 대출·세금 기준, FAQ 수치는 그대로입니다.
- 기존 함수는 하나도 빠지지 않았습니다(추가 14개).
