# TrackApt — PROJECT.md

전국 아파트 가격 서열·타이밍·자금 분석 웹앱. 단일 HTML + Vercel 서버리스 프록시.
배포: https://track-apt.vercel.app · 저장소: TrackApt

## 구조

```
index.html          앱 전체 (탭 UI + 계산 엔진 + Chart.js)
kb_data.json        KB 월간 시계열 가공본 (~0.95MB)
api/trades.js       국토부 매매(RTMSDataSvcAptTradeDev)·전월세(AptRent) 프록시
api/apt.js          K-apt 단지목록·기본·상세 프록시 (엔드포인트 폴백 내장)
api/rent.js         전월세 단독 프록시 (trades.js kind=rent와 동일 계열)
api/supply.js       청약홈 분양정보(odcloud) 프록시 — 입주물량 추정용
```

Vercel 환경변수: `MOLIT_API_KEY` (공공데이터포털 일반 인증키 1개로 전부 커버).
활용신청 필요 서비스: ① 아파트 매매 실거래가 상세 ② 아파트 전월세 실거래가
③ 공동주택 단지 목록/기본정보 ④ 청약홈 분양정보 조회 서비스.
카카오맵: index.html 상단 `KAKAO_JS_KEY` 상수에 JavaScript 키를 넣어둔다
(현재 값: 2d591b6ef9c70f94f369b7c55c91baa3). 비워두면 앱 내 입력 UI가 뜨고
localStorage에 저장된다. JS 키는 도메인 제한으로 보호되므로 소스 포함이 안전하나,
카카오 콘솔 → 내 애플리케이션 → 플랫폼 → Web에 배포 도메인 등록이 필수다.
(REST API 키·Admin 키는 절대 소스에 넣지 않는다.)

## 데이터 파이프라인

### KB 월간 시계열 (매달 수동 갱신)
1. KB부동산 데이터허브 → KB통계 → 통계자료실 → 월간통계 → `(YYYY.MM)월간시계열.xlsx` 다운로드
2. 변환 스크립트(convert.py, 대화 세션에 보관)로 kb_data.json 생성
   - 사용 시트: 2.매매APT / 6.전세APT / 47·48.㎡당 평균가 / 21.매수우위 / 23.전세수급
   - 날짜 파싱: `86.1`·`'00.1`·`2013.4` 세 표기 모두 처리, 연속성 검증 포함
3. GitHub에서 kb_data.json 교체 → Vercel 자동 재배포

### 주의: 시트 간 지역명 표기 차이
지수 시트(매매/전세APT)는 `강남 11개구`(공백), ㎡당가 시트는 `강남11개구`.
앱의 `ik()` 함수가 공백 제거 + '시' 접미사 폴백으로 매핑한다.
새 데이터에서 지역이 늘면 이 매핑이 깨지지 않는지 데이터 상태 패널로 확인.

### 지수 기준시점
2026.07 파일 기준 2026.1=100 (개편됨). 앱은 상승률·배율·백분위 등
상대값만 쓰므로 기준시점 변경의 영향이 없다.

## 핵심 계산 규칙

- 필요현금 = 매수가 − min(LTV, 총액상한, DSR) + 취득세 + 중개보수 + 부대비
- 총액상한: 15억↓ 6억 / 15~25억 4억 / 25억↑ 2억 (수도권·규제, 기준·설정에서 수정)
- 추가매수: 수도권·규제지역 주담대 0
- DSR: 스트레스 가산 포함 원리금균등 역산, 연소득 0이면 미적용
- 연도별 상승률: 12월 지수 기준, 마지막 해는 최신월
- 밴드 백분위: 최근 120개월 분포, 표본 24개월 미만은 "표본 부족" 처리
- 추천 5축: 급지35 · 성과25(10년 연복리 7.2%=만점) · 수급15(20~100) ·
  타이밍15(40~100) · 자금10 — 가중치는 기준·설정에서 수정
- 장바구니 6축: 급지25 · 성과20 · 타이밍15 · 수급10 · 단지15 · 자금15
- 백테스트: 10년 전 같은 분기 3개월 샘플링, 분위(상위20%=대장, 40~60%=중간),
  이름 매칭 생존 편향 주의문 포함

## 법정동코드(LAWD)

108개 지역 내장. 미등록(2026 신설 인천 제물포·영종·검단·서해, 화성 4구, 도 단위)은
단지 찾기·거래량 탭의 "직접 입력"으로 조회. 코드가 확정 공표되면 LAWD 객체에 추가.

## 버전 이력

- v0.1~0.2 뼈대 + KB 데이터 가공
- v0.3 시장 현황 (연도별·지수·2년 복리)
- v0.4 급지표·서열 사다리·자금 엔진·기준 설정 (규제 37곳 편집형)
- v0.5 갭투자 밴드 · 갈아타기 배율 (+표본 부족 처리)
- v0.6 맞춤 추천 5축 + 분위 백테스트 근거
- v0.7 단지 찾기 (실거래 + K-apt), 장바구니
- v0.8 이미지·엑셀 내보내기, 장바구니 6축 채점 보고서
- v0.9 4분면 · 순환매 · 자금 계산기 · 지수 키 정규화 버그 수정(ik)
- v0.10 거래량 국면 판정 · 단지 전세/갭
- v0.11 대장 아파트 · 단지 비교 (지역 캐시 공유)
- v0.12 급지 지도 (카카오맵, 좌표 108곳 내장)
- v0.13 수급 (청약홈) · 백테스트 (ymShift 음수 버그 수정)
- v0.14 밴드 미니 바 · 갈아타기 읽기 · 추천 카드 개편 · 판정 태그 · 2년 KPI
- v1.0 사용법 · 지표 사전 · 파비콘 · 모바일 마감

## 알려진 한계

- KB 표본조사 특성, 표본 개편 단절(2022.11 · 2026.4)
- 실거래 최근 1~2개월 과소집계 (신고 기한 30일) — 거래량 판정에서 최신월 제외
- 수급 탭은 분양 공고 기반 과소 추정 가능 (공공임대·후분양 일부 누락)
- 호재(노선·재건축·일자리)는 데이터에 없음 — 순환매·갈아타기 판단 시 수동 확인 필수

## 향후 아이디어

- Capacitor 패키징 (App ID 후보: com.hmjeon.trackapt — 하이픈 없음)
- 읍면동 단위 4분면 (실거래 직접 집계)
- 다크 모드

## v1.5 내비게이션 구조

6개 대분류 + 하위 탭 (GROUPS 배열로 정의):
- 시작하기: 추천 받기 · 내 조건 · 사용법
- 시장 공부: 시장 흐름 · 거래량 · 매매·전세 흐름 · 입주 물량 · 과거 검증
- 어디를 살까: 지역 순위 · 지도로 보기 · 전세 끼고 사기 · 갈아타기 · 다음 오를 곳
- 어떤 아파트: 지역 1등 아파트 · 아파트 찾기 · 아파트 비교
- 돈·설정: 돈 계산 · 설정
- 내 후보: 최종 검토 (담은 수가 상단 배지에 표시)

탭 추가 시 GROUPS에 { id, name, build } 한 줄만 넣으면 되고,
showTab의 렌더 디스패치에도 한 줄 추가한다.

## v2.3 성능·안정화

- TRADE_MEMO: (kind|lawd|ym) 단위 실거래 응답 메모리 캐시. 지난달 데이터는 확정치라
  세션 내 재사용. 같은 지역 재조회·탭 간 이동 시 네트워크 0회.
- KAPT_INFO_MEMO: 단지 코드별 K-apt 상세 캐시.
- 단지 찾기: 실거래 도착 즉시 표 렌더 → K-apt 부착은 백그라운드 병렬(apiGet 큐가
  동시 3개 제한), 0.5초 배치로 표 갱신. DJ.loadSeq로 이전 조회의 늦은 응답 무시.
- KB 기반 탭(전세끼고/갈아타기/다음오를곳)의 지역 목록: salePm 표본 24(rota 36)개월
  미만 지역 제외, 하위 구가 전부 빠지면 상위 시로 대체(화성 신설구 → 화성시).
- 데이터 구조 정리: KB 시계열은 kb_data.json 정적 파일(빌드 시 1회 로드),
  실거래·K-apt·청약홈만 실시간 API(캐시 적용).

## v2.4 다크 테마 · 지역 데이터 정합

- 다크 럭셔리 팔레트: --bg #0b1017 / --panel #131a24 / 골드 --brand #d4af6a
  (--gold-grad), 상승 #2fbf9b, 하락 #e08c3a. 활성 버튼·주요 CTA는 골드 그라데이션,
  브랜드 로고는 그라데이션 텍스트. 카드에 inset 하이라이트+딥 섀도.
  Chart.js 전역 색, 레이더 그리드(#2a3546), 4분면 라벨, 지도 핀(hsl 40 골드톤),
  이미지 내보내기 배경(#0b1017)까지 다크 통일.
- RANKED: salePm 표본 24개월 미만 지역 제외, 정상 하위 구가 없으면 상위 시 유지
  → 화성 신설구 대신 화성시(160개월)가 추천·순위·밴드 대상.
- LAWD_GROUPS "화성시 전체" → "화성시"로 개명해 buildLawdMap 매핑 정상화
  (화성시가 '전국·권역'으로 새던 버그 수정).
- 모바일: .seg flex 1줄 균등폭, 셀렉트 font-size 16px(iOS 확대 방지)+ellipsis,
  720px 이하에서 세그 폰트 축소·버튼 전폭·stepbar 줄바꿈.

## v2.8 화성 분구 실거래 병합 조회

- 문제: 2026.2 화성 분구 후 실거래가 옛 코드(41590)로 오는지 신설 구 코드로
  갈라졌는지 공표 불확실 → 41590 단일 조회 시 빈 결과 가능성.
- 해결: fetchHwa() — 2026.2 이전 달은 41590만, 이후 달은 41590 + 신설 후보
  (41591~41594) 중 첫 조회에서 실데이터를 낸 코드(HWA.live, 세션당 1회 탐지)를
  함께 조회해 mergeTradeResults()로 병합(이중 신고 dedupe: ym|day|apt|dong|floor|금액|area).
- K-apt 목록도 fetchKaptList()로 옛+신설 코드 병합 (KAPT_LIST_MEMO 캐시).
- fetchTrade 진입점에서 첫 후보가 41590이면 자동으로 병합 경로를 타므로
  단지찾기·대장·거래량·비교·백테스트 전부 커버.
- 신설 구 코드가 확정 공표되면 HWA.news 배열만 실제 코드로 교체하면 된다.
