# TrackApt — PROJECT.md

전국 아파트 가격 서열·타이밍·자금 분석 웹앱. 단일 HTML + Vercel 서버리스 프록시.
배포: https://track-apt.vercel.app · 저장소: TrackApt

> **읽는 순서 주의** — 각 절은 **최신이 위, 오래된 것이 아래**다. 파일 끝의 "…이 필요하다"
> 류 문장은 대개 **이미 해결된 옛 진단**이다. 남은 할 일은 HANDOFF.md의 백로그에서 본다.
> 데이터 상태는 문장이 아니라 `market_core.json`의 `stat`(rentBlank 등)으로 확인할 것.

## 구조

```
index.html          앱 전체 (탭 UI + 계산 엔진 + Chart.js)
kb_data.json        KB 월간 시계열 가공본 (~0.95MB)
api/trades.js       국토부 매매(RTMSDataSvcAptTradeDev)·전월세(AptRent) 프록시
api/apt.js          K-apt 단지목록·기본·상세 프록시 (엔드포인트 폴백 내장)
api/rent.js         전월세 단독 프록시 (trades.js kind=rent와 동일 계열)
api/supply.js       청약홈 분양정보(odcloud) 프록시 — 입주물량 추정용
```

Vercel 환경변수: `MOLIT_API_KEY` (공공데이터포털 일반 인증키 1개로 전부 커버).
활용신청 필요 서비스: ① 아파트 매매 실거래가 상세 ② 아파트 전월세 실거래가
③ 공동주택 단지 목록/기본정보 ④ 청약홈 분양정보 조회 서비스.
카카오맵: index.html 상단 `KAKAO_JS_KEY` 상수에 JavaScript 키를 넣어둔다
(현재 값: 2d591b6ef9c70f94f369b7c55c91baa3). 비워두면 앱 내 입력 UI가 뜨고
localStorage에 저장된다. JS 키는 도메인 제한으로 보호되므로 소스 포함이 안전하나,
카카오 콘솔 → 내 애플리케이션 → 플랫폼 → Web에 배포 도메인 등록이 필수다.
(REST API 키·Admin 키는 절대 소스에 넣지 않는다.)

## 데이터 파이프라인

### KB 월간 시계열 (매달 수동 갱신)
1. KB부동산 데이터허브 → KB통계 → 통계자료실 → 월간통계 → `(YYYY.MM)월간시계열.xlsx` 다운로드
2. 변환 스크립트(convert.py, 대화 세션에 보관)로 kb_data.json 생성
   - 사용 시트: 2.매매APT / 6.전세APT / 47·48.㎡당 평균가 / 21.매수우위 / 23.전세수급
   - 날짜 파싱: `86.1`·`'00.1`·`2013.4` 세 표기 모두 처리, 연속성 검증 포함
3. GitHub에서 kb_data.json 교체 → Vercel 자동 재배포

### 주의: 시트 간 지역명 표기 차이
지수 시트(매매/전세APT)는 `강남 11개구`(공백), ㎡당가 시트는 `강남11개구`.
앱의 `ik()` 함수가 공백 제거 + '시' 접미사 폴백으로 매핑한다.
새 데이터에서 지역이 늘면 이 매핑이 깨지지 않는지 데이터 상태 패널로 확인.

### 지수 기준시점
2026.07 파일 기준 2026.1=100 (개편됨). 앱은 상승률·배율·백분위 등
상대값만 쓰므로 기준시점 변경의 영향이 없다.

## 핵심 계산 규칙

- 필요현금 = 매수가 − min(LTV, 총액상한, DSR) + 취득세 + 중개보수 + 부대비
- 총액상한: 15억↓ 6억 / 15~25억 4억 / 25억↑ 2억 (수도권·규제, 기준·설정에서 수정)
- 추가매수: 수도권·규제지역 주담대 0
- DSR: 스트레스 가산 포함 원리금균등 역산, 연소득 0이면 미적용
- 연도별 상승률: 12월 지수 기준, 마지막 해는 최신월
- 밴드 백분위: 최근 120개월 분포, 표본 24개월 미만은 "표본 부족" 처리
- 추천 5축: 급지35 · 성과25(10년 연복리 7.2%=만점) · 수급15(20~100) ·
  타이밍15(40~100) · 자금10 — 가중치는 기준·설정에서 수정
- 장바구니 6축: 급지25 · 성과20 · 타이밍15 · 수급10 · 단지15 · 자금15
- 백테스트: 10년 전 같은 분기 3개월 샘플링, 분위(상위20%=대장, 40~60%=중간),
  이름 매칭 생존 편향 주의문 포함

## 법정동코드(LAWD)

108개 지역 내장. 미등록(2026 신설 인천 제물포·영종·검단·서해, 화성 4구, 도 단위)은
단지 찾기·거래량 탭의 "직접 입력"으로 조회. 코드가 확정 공표되면 LAWD 객체에 추가.

## 버전 이력

- v0.1~0.2 뼈대 + KB 데이터 가공
- v0.3 시장 현황 (연도별·지수·2년 복리)
- v0.4 급지표·서열 사다리·자금 엔진·기준 설정 (규제 37곳 편집형)
- v0.5 갭투자 밴드 · 갈아타기 배율 (+표본 부족 처리)
- v0.6 맞춤 추천 5축 + 분위 백테스트 근거
- v0.7 단지 찾기 (실거래 + K-apt), 장바구니
- v0.8 이미지·엑셀 내보내기, 장바구니 6축 채점 보고서
- v0.9 4분면 · 순환매 · 자금 계산기 · 지수 키 정규화 버그 수정(ik)
- v0.10 거래량 국면 판정 · 단지 전세/갭
- v0.11 대장 아파트 · 단지 비교 (지역 캐시 공유)
- v0.12 급지 지도 (카카오맵, 좌표 108곳 내장)
- v0.13 수급 (청약홈) · 백테스트 (ymShift 음수 버그 수정)
- v0.14 밴드 미니 바 · 갈아타기 읽기 · 추천 카드 개편 · 판정 태그 · 2년 KPI
- v1.0 사용법 · 지표 사전 · 파비콘 · 모바일 마감

## 알려진 한계

- KB 표본조사 특성, 표본 개편 단절(2022.11 · 2026.4)
- 실거래 최근 1~2개월 과소집계 (신고 기한 30일) — 거래량 판정에서 최신월 제외
- 수급 탭은 분양 공고 기반 과소 추정 가능 (공공임대·후분양 일부 누락)
- 호재(노선·재건축·일자리)는 데이터에 없음 — 순환매·갈아타기 판단 시 수동 확인 필수

## 향후 아이디어

- Capacitor 패키징 (App ID 후보: com.hmjeon.trackapt — 하이픈 없음)
- 읍면동 단위 4분면 (실거래 직접 집계)
- 다크 모드

## v1.5 내비게이션 구조

6개 대분류 + 하위 탭 (GROUPS 배열로 정의):
- 시작하기: 추천 받기 · 내 조건 · 사용법
- 시장 공부: 시장 흐름 · 거래량 · 매매·전세 흐름 · 입주 물량 · 과거 검증
- 어디를 살까: 지역 순위 · 지도로 보기 · 전세 끼고 사기 · 갈아타기 · 다음 오를 곳
- 어떤 아파트: 지역 1등 아파트 · 아파트 찾기 · 아파트 비교
- 돈·설정: 돈 계산 · 설정
- 내 후보: 최종 검토 (담은 수가 상단 배지에 표시)

탭 추가 시 GROUPS에 { id, name, build } 한 줄만 넣으면 되고,
showTab의 렌더 디스패치에도 한 줄 추가한다.

## v2.3 성능·안정화

- TRADE_MEMO: (kind|lawd|ym) 단위 실거래 응답 메모리 캐시. 지난달 데이터는 확정치라
  세션 내 재사용. 같은 지역 재조회·탭 간 이동 시 네트워크 0회.
- KAPT_INFO_MEMO: 단지 코드별 K-apt 상세 캐시.
- 단지 찾기: 실거래 도착 즉시 표 렌더 → K-apt 부착은 백그라운드 병렬(apiGet 큐가
  동시 3개 제한), 0.5초 배치로 표 갱신. DJ.loadSeq로 이전 조회의 늦은 응답 무시.
- KB 기반 탭(전세끼고/갈아타기/다음오를곳)의 지역 목록: salePm 표본 24(rota 36)개월
  미만 지역 제외, 하위 구가 전부 빠지면 상위 시로 대체(화성 신설구 → 화성시).
- 데이터 구조 정리: KB 시계열은 kb_data.json 정적 파일(빌드 시 1회 로드),
  실거래·K-apt·청약홈만 실시간 API(캐시 적용).

## v2.4 다크 테마 · 지역 데이터 정합

- 다크 럭셔리 팔레트: --bg #0b1017 / --panel #131a24 / 골드 --brand #d4af6a
  (--gold-grad), 상승 #2fbf9b, 하락 #e08c3a. 활성 버튼·주요 CTA는 골드 그라데이션,
  브랜드 로고는 그라데이션 텍스트. 카드에 inset 하이라이트+딥 섀도.
  Chart.js 전역 색, 레이더 그리드(#2a3546), 4분면 라벨, 지도 핀(hsl 40 골드톤),
  이미지 내보내기 배경(#0b1017)까지 다크 통일.
- RANKED: salePm 표본 24개월 미만 지역 제외, 정상 하위 구가 없으면 상위 시 유지
  → 화성 신설구 대신 화성시(160개월)가 추천·순위·밴드 대상.
- LAWD_GROUPS "화성시 전체" → "화성시"로 개명해 buildLawdMap 매핑 정상화
  (화성시가 '전국·권역'으로 새던 버그 수정).
- 모바일: .seg flex 1줄 균등폭, 셀렉트 font-size 16px(iOS 확대 방지)+ellipsis,
  720px 이하에서 세그 폰트 축소·버튼 전폭·stepbar 줄바꿈.

## v2.8 화성 분구 실거래 병합 조회

- 문제: 2026.2 화성 분구 후 실거래가 옛 코드(41590)로 오는지 신설 구 코드로
  갈라졌는지 공표 불확실 → 41590 단일 조회 시 빈 결과 가능성.
- 해결: fetchHwa() — 2026.2 이전 달은 41590만, 이후 달은 41590 + 신설 후보
  (41591~41594) 중 첫 조회에서 실데이터를 낸 코드(HWA.live, 세션당 1회 탐지)를
  함께 조회해 mergeTradeResults()로 병합(이중 신고 dedupe: ym|day|apt|dong|floor|금액|area).
- K-apt 목록도 fetchKaptList()로 옛+신설 코드 병합 (KAPT_LIST_MEMO 캐시).
- fetchTrade 진입점에서 첫 후보가 41590이면 자동으로 병합 경로를 타므로
  단지찾기·대장·거래량·비교·백테스트 전부 커버.
- 신설 구 코드가 확정 공표되면 HWA.news 배열만 실제 코드로 교체하면 된다.

## v3.1 FAQ 탭 · 입주물량 자동 조회

- 시작하기 그룹에 "FAQ · 데이터 검증" 탭 신설 (buildFaq). 참고 앱의 Q1~Q17
  백테스트 결과를 아코디언(details.faq)으로 수록 — 각 문항에 결론·표·근거·출처,
  요약 10줄, 신뢰도 경고 5항목 포함. 스타일: .fq 골드 배지, fbody 본문.
- 입주물량 탭: showTab("supply") 시 autoLoadSupply() — 내 지역의 시·도를
  kbSido로 유도해 기본 선택 후 자동 조회(세션 1회, SP.loading 가드).
  시·도 변경 시 즉시 재조회. 활용 노트(입주 전후 6개월 전세 압박, 미분양 vs
  입주물량 차이) 추가.

## v3.2 입주물량 전국 집계 기본

- 참고 앱과 동일하게 최초 접속(탭 진입) 시 "전국" 자동 조회. 17개 시·도를
  병렬 호출(SUPPLY_MEMO 캐시, apiGet 동시 3 제한) 후 병합. items에 sido 태깅.
- KPI 5종: 향후 12개월 물량 / 24개월 누적 / 연간 추정 수요(전국 인구 합 × 0.5%,
  부제로 인구 표기 — statBox 4번째 인자 sub 추가) / 수급 배율 / 공고 기준(+실패 시·도).
- "수급 읽기" 자동 요약(#sp-read): 배율 판정 문장, (전국일 때) 시·도별 배율
  상·하위 3곳, 물량 몰리는 시·군·구 top3 + 입주 전후 전세 압박 해설.
- 시·군·구 표는 전국 모드에서 "경기 평택시" 형태 시·도 접두. 시·도 전환 시
  즉시 재조회(캐시 적중 시 네트워크 0회). 청약홈 API 실시간 → 지속 최신화.

## v3.3 FAQ 라이브 재계산

- Q1(분위 10년)·Q2(모멘텀 역전)·Q12(낙폭·회복)·Q15(국면별 3분위)는 내장 KB
  시계열만으로 재현 가능 → faqCompute()가 재계산해 fb-q* 컨테이너에 주입.
  계산 43ms. Q2는 장기 지수(sale, 1986~) 4개월 간격, Q15 국면 경계는 전국
  지수 저점·고점 자동 판정.
- 재계산 주기 설정(LS_FAQ_PERIOD): 데이터 갱신 시 자동(기본)/7일/30일/항상.
  캐시 LS_FAQ_CACHE에 dataYm 포함 — kb_data.json의 최신 월이 바뀌면 주기와
  무관하게 자동 재계산. "지금 다시 계산" 버튼, fq-status에 기준·시각 표시.
- 문항 배지: 라이브(Q1·Q2·Q12·Q15) / 고정 검증(나머지 13개 — 실거래 10년치·
  미분양·금리 등 별도 원자료 필요, 2026.07 검증 고정).

## v3.4 확장 수급 데이터(supply_data.json) · 출처 표

- FAQ 재계산 주기 UI 제거 — kb_data.json 교체 시(최신 월 변경 감지) 자동
  재계산 + "지금 다시 계산" 버튼만 유지. 사용자가 파일을 넣는 워크플로우에 맞춤.
- SUPPLY_EXT: supply_data.json(선택)을 시작 시 로드. 없으면 청약홈만으로 동작
  + 입주물량 탭에 안내(sp-extmiss). 있으면:
  · 미분양 추이 카드(sp-unsoldcard): 월별 라인 + 최고·최저·현위치%·준공후
    미분양 자동 해설. 지역 전환 연동.
  · 공급·수요·매매가격 연도별 카드(sp-yearcard): 준공실적 bar + 입주예정 bar
    + 수요 점선 + KB 매매지수 line(우축, SIDO_IDX 매핑). 준공 사이클 해설
    (연평균·수요 대비 배수·최근 3년 vs 장기).
  · 시군구 표 확장: 인구·추정 수요·자체 배율(1.3↑ hit)·준공후 미분양 열.
- supply_data.json 스키마 (견본: supply_data.sample.json):
  { updated, unsold:{dates:[from,to,n],series:{지역:{o,v}}}, unsoldDone:동일,
    completions:{years:[..],sido:{전국/시도:[..]}},
    expected:{items:[{sido,sigungu,ym,households}]},
    population:{total,sido:{},sigungu:{"경기 평택시":620754,...}} }
  지역 키: "전국" / 시도 축약("경기") / 시군구 "경기 평택시".
- 갱신 절차: ① 통계누리 미분양주택현황(시군구 월별) 엑셀 ② 국토부 준공실적
  ③ 부동산원 입주예정물량 CSV ④ 행안부 주민등록 인구 CSV를 세션에 업로드
  → supply_data.json으로 변환해 전달(교체 업로드) — kb_data.json과 동일 방식.
- 사용법 탭 끝에 "쓴 자료 전체" 출처 표(guideSourceTable): 자료·출처·범위·갱신
  방식(자동 API/내가 교체/미장착)을 실시간 상태로 표시.

## v3.5 화성 코드 확정 · 데이터 업로드 탭 · supply_data 실데이터

- **화성 분구 코드 확정(탐지 로직 제거)**: 행안부 주민등록 행정구역 코드로
  41591 만세구 · 41593 효행구 · 41595 병점구 · 41597 동탄구 확정.
  (41592 동부출장소 · 41594 동탄출장소는 구가 아니라 제외.)
  `HWA.guOf` 정적 매핑으로 대체하고 `probeHwaCodes`·`guessHwaGu`·`HWA_ANCHOR` 삭제
  → 세션당 8회 탐지 호출이 사라졌다. 분구 전(202602 미만) 달은 41590만 조회.
  안전장치: 신설 코드 병합 결과가 0건이면 41590으로 1회 재조회(이관 지연 대비).
- **설정 탭의 "지역 코드 진단" 제거** (`runDiag` + UI). 목적을 다했다.
- **데이터 관리 탭 신설** (돈·설정 → 데이터 관리, `buildData`):
  · kb_data.json / supply_data.json을 브라우저에서 직접 업로드 → IndexedDB
    (`trackapt/files`)에 보관, 다음 방문에도 유지. GitHub 재배포 없이 교체 가능.
  · 우선순위: IndexedDB 저장본 > 저장소 정적 파일. [올린 파일 지우기]로 원복.
  · 업로드 전 형식 검사(`validateKbFile`/`validateSupplyFile`) — 실패 시 저장하지
    않고 사유 표시(기존 데이터 보존). 12MB 초과 거부.
  · `DATA_SRC = {kb, supply}`(none|file|upload)로 현재 출처를 탭 상단에 표시.
  · 저장 후 새로고침 여부를 확인받아 적용.
- **supply_data.json 실데이터화** (견본 → 실제):
  · unsold: 통계누리 시군구별 미분양 2010-01~2026-06(198개월), 256개 지역
    (전국 = 시도 '계' 합산). 준공후 미분양(unsoldDone)은 원자료에 없어 미장착.
  · completions: 통계누리 준공 월계(구분=총계·부문=총계) 연간 합산,
    12개월이 모두 있는 해만 채택 → 2011~2025.
  · expected: 부동산원·R114 2026.2.27 보도자료의 시·도별 2026·2027 연간 물량
    (시군구 단위는 청약홈 API가 담당).
  · population: 행안부 주민등록 2026.06 시도 17곳 + 시군구 267곳(출장소 제외).
  · 생성 스크립트 `make_supply.py` 동봉 — 원자료 CSV(cp949) 4종을 같은 폴더에
    두고 실행하면 재생성된다.
- 시군구 표: `unsoldDone`이 없으면 `unsold`로 대체 표시(열 이름 "미분양"),
  "수원시 영통구"처럼 구 단위 키는 시 단위("수원시")로 되짚어 매칭.

### v3.5 알려진 공백
- 준공후 미분양은 통계누리의 별도 통계(준공후 미분양 현황)라 추가 다운로드 필요.
- 업로드 데이터는 브라우저 로컬(IndexedDB) — 기기·브라우저마다 따로. 모든 기기에
  반영하려면 저장소의 파일 자체를 교체해야 한다.

## v3.6 지역 선택기 · 규제지역 최신화 · 응답 지연 방어

- **지역 선택기(접기/펴기)**: optgroup이 있는 모든 지역 셀렉트를 버튼 + 아코디언
  모달로 자동 승격(`upgradeRegionSelects`). 시·도를 접었다 펴고 검색으로 걸러낸다.
  원본 `<select>`는 값 보관용으로 숨겨둔 채 남기고 `selectedIndex`를 바꾼 뒤
  change를 발생시키므로 각 탭 코드는 무수정. **value가 아니라 index로 선택**하는
  것이 핵심 — 화성 4구는 value가 모두 41590이라 value로는 구분이 안 된다.
  `refreshHomeMarks()`가 래퍼가 되어 셀렉트가 다시 채워질 때마다 버튼 라벨도 갱신.
- **규제지역 2026.7.1 기준으로 갱신**: 서울 25개 자치구 + 경기 15곳.
  기존 12곳(과천·광명·성남 분당/수정/중원·수원 영통/장안/팔달·안양 동안·
  용인 수지·의왕·하남)에 **화성시 동탄구·용인시 기흥구·구리시** 추가
  (2026.6.30 주정심 의결, 7.1 지정효력 / 7.5 토지거래허가구역).
  `RULES.regVer`(20260701) 도입 — 저장된 기준일이 낮으면 localStorage 저장본 대신
  새 기본 목록으로 갈아끼운다. 사용자가 직접 편집한 목록도 이때 한 번 초기화된다.
- **API 응답 지연 방어**: `fetchTimeout`(20초, AbortController)을 apiGet 경로에 적용.
  응답이 영영 안 오면 동시 3개 큐가 막혀 이후 모든 조회가 "불러오는 중…"에서
  멈추던 문제(채점 보고서 주변환경 불러오기 포함) 해소. 초과 시 재시도 → 오류 표시.
- **사용법 출처 표**: 부팅 직후 빌드 시점엔 supply_data 로드 전이라 항상 "미장착"으로
  보이던 버그 수정. `#g-sources` 컨테이너로 분리해 탭 진입 때마다 다시 그린다.

## v3.7 원본 CSV 자동 변환 · 주변환경 재조회 결과 표시

- **CSV → JSON 브라우저 변환**: 데이터 관리 탭의 [원본 CSV로 만들기]로 통계누리·
  행안부 CSV를 여러 개 한꺼번에 올리면 브라우저가 종류를 판별해 supply_data를
  만들어 저장한다. 파이썬 변환기(make_supply.py)와 **동일 결과**임을 테스트로 확인.
  · `decodeKr`: TextDecoder("euc-kr") → 깨짐 많으면 UTF-8 재시도.
  · `csvKind`: 헤더로 unsold/completions/population 판별(단서 없으면 데이터 모양).
  · `ymPick`: "2025-10(잠정 p)" 같은 잠정치 표기에서 연-월만 추출 — 이걸 안 하면
    준공 2025년이 9개월로 잡혀 통째로 빠진다.
  · 올린 종류의 블록만 교체하고 나머지(특히 CSV로 안 나오는 expected)는 보존.
  · kb_data는 원본이 다중 시트 엑셀이라 브라우저 변환 대상이 아니다(세션 변환 유지).
- **주변환경 재조회**: K-apt 상세를 받아왔는데 교통·학군·편의 항목이 모두 비어
  있으면 이전엔 같은 "지금 불러오기" 링크로 되돌아와 무한 반복처럼 보였다.
  성공 시 `envTried` 플래그를 남겨 "등록은 돼 있으나 항목이 비어 있음"으로 안내.
- 데이터 관리 탭에 엣지·사파리 **추적 방지**로 IndexedDB 저장이 막힐 수 있다는 안내
  추가 (콘솔의 "Tracking Prevention blocked access to storage" 경고 대응).

## v3.8 데이터 업로드 탭 제거 (저장소 파일 단일 경로)

- kb_data의 원본이 다중 시트 엑셀이라 브라우저 변환이 불가능 → 두 파일의 갱신
  경로가 갈리는 것보다 **한 갈래로 통일**하는 편이 낫다는 판단으로 업로드 기능 철수.
- 제거 대상: 데이터 관리 탭(`buildData`·`renderDataTab`·`dataCard`·`validateKbFile`·
  `validateSupplyFile`·`onDataFile`·`clearDataFile`), IndexedDB 계층(`idbOpen`/`idbGet`/
  `idbSet`/`idbDel`·`DATA_SRC`·`dataMeta`), CSV 변환기(`buildSupplyFromCsv`·`csvKind`·
  `decodeKr`·`ymPick`·`parseUnsold`/`parseCompletions`/`parsePopulation`).
- `loadData`·`loadSupplyExt`는 v3.4 방식(정적 파일 fetch)으로 원복.
- **확정 갱신 절차**: 원본 CSV/엑셀을 대화 세션에 업로드 → 변환본(kb_data.json ·
  supply_data.json)을 받아 → GitHub 웹 UI로 교체 → Vercel 자동 배포.
  변환 스크립트는 `make_supply.py`(수급) / convert.py(KB, 세션 보관).
- v3.7의 나머지 개선(주변환경 `envTried` 표시)은 그대로 유지.

## v3.9 과거 검증 4개 모드 · 전국 등급표(10분위) · 전앱 % 표시 버그 수정

- **[치명 버그] pct 이름 충돌 수정**: v3.3 FAQ 재계산에 넣은 분수 입력용
  `pct(x)`가 전역으로 선언돼, 함수 호이스팅으로 원래 `pct(v)`(퍼센트 입력)를
  **앱 전체에서 덮어썼다**. v3.3~v3.8의 모든 % 표시(성과/년, 10년 상승률,
  판정 문장 등)가 100배 부풀려져 있었다 ("+549.4%/년" → 실제 +5.5%/년).
  비교·판정은 원시값으로 해서 서열·등급 자체는 정상이었고 표시만 오염.
  FAQ 쪽을 `fpct`로 개명(호출 8곳 치환)해 해결.
- **과거 검증 4개 모드** (`BT_MODES`): 평당가 순위(기존) / 세대수(나홀로<300 vs
  대단지≥1000, K-apt 세대수 — 후보 상위 40개 단지로 호출 상한) / 평형(소형<55 ·
  중형55~95 · 대형≥95, 평형 선택 무시하고 전체 거래, 단지×평형대로 그룹) /
  노후도(검증 시작 시점 연차 — 신축<10 · 준구축10~24 · 구축≥25, 실거래
  buildYear 사용이라 추가 조회 없음). 검증 기간 5·10·15년 선택.
  판정 쌍: rank A중간vsB대장 · size A나홀로vsB대단지 · area A소형vsB대형 ·
  age A구축vsB신축. `groupComplexesBt`가 모드별 그룹핑 담당.
- **전국 등급표 탭** (`tiers`, 어디를 살까 그룹): KB만으로 계산(API 0회).
  RANKED를 10등분(1분위=상위 10%), 시·도 칩으로 필터해도 서열·분위는 전국 기준
  유지. 컬럼: 서열·분위·평당가·84/59㎡ 매매·전세·전세가율·밴드(120개월)·3년·
  10년(+연복리)·10Y CAGR·전체 CAGR(지수 기산점부터, `fullCagr`)·100% 합격·규제.
  KPI: 통과 수 / 서울·전국 10년(+CAGR) / 통과 기준. "시·도 단위" 칩은 시도급
  KB 키(`tierSidoKeys`)를 자체 서열로. 1~3분위 필터 · 84/59 토글 · 분위 배지 CSS.

## v3.9 감사 기록 (2026.08) — 프로세스·성능·데이터 정확성

코드 수정 없이 통과. 세 갈래로 실측 검증했다 (하네스: audit_net/perf/data.js, 세션 보관).

### ① 네트워크 (이벤트별 호출 감사)
- KB 전용 14개 탭(시장/순위/등급표/갭/갈아타기/로타/4분면/계산/설정/사용법/FAQ/지도/시작/추천): **API 0회**.
- 입주물량: 최초 17회(시도 병렬) → 재진입 0회. 아파트 찾기: 6개월 조회 15회
  (매매6+전세6+목록1+상세2) → 같은 조건 재조회 0회.
- **탭 간 캐시 공유 확인**: 같은 지역을 거래량 24개월로 조회하면 아파트 찾기가
  이미 받은 6개월을 빼고 18회만 호출. 대장 아파트는 전량 캐시로 0회.
- 백테스트 30회(2지역×[과거3+현재 매매6·전세6]) → 재실행 0회. 중복 URL 0건.
- 화성: 분구 후 4회(구 코드만, 41590 재조회 없음) → 메모 적중 0회, 분구 전 1회.
- 동시 호출 최대 3 (큐 제한 준수).

### ② 성능
- 계산 자체는 가벼움: bandStats×120지역 3.4ms · fullCagr 0.4ms · idxChange 0.9ms ·
  faqCompute 8.6ms. 렌더 시간은 jsdom 기준 100~500ms지만 대부분 DOM 조립 오버헤드로
  실브라우저 추정 30~100ms — 병목 없음, 수정 불필요 판단.

### ③ 데이터 정확성 (독립 재계산 대조)
- **RANKED 120곳**: 파이썬으로 제외 규칙(집계 그룹·표본<24·하위구 있는 상위 숨김)과
  정렬을 재현 → 순서까지 완전 일치. 화성 신설구 제외·화성시 포함·수원시 숨김 확인.
- **지표값**: 표본 지역(서울 구 3·분당·화성·평택·영통)의 평당가/3·10년 상승률/밴드
  백분위/전체 CAGR — 소수점까지 일치. ik()의 공백 정규화로 '강남11개구|강남구'(㎡당가)
  ↔ '강남 11개구|강남구'(지수) 매핑 정상.
- **finance()**: 규제 12억→LTV 4.8억 / 20억→상한 4억 / 30억→상한 2억 / 비규제
  6억→4.2억 / 규제·수도권 추가매수(add·multi)→대출 0 / multi 취득세 12% / DSR은
  소득 반영 시 감소·양수 — 전부 규칙표와 일치.
- **supply_data.json**: 미분양 무작위 10표본(지역×월)을 원본 CSV에서 재집계해 대조
  → 전부 일치(전국=시도 합 포함). 준공 4표본(경기2018·전국2025·부산2015·서울2021)
  월합 일치. 입주예정 2026 합 198,583 / 2027 합 216,323 — 보도자료 '계'와 일치.
- **유틸**: mergeTradeResults 중복 신고 dedupe(3→2), 그룹 중위수 평당 산식 일치.
- **규제 40곳**: 서울 25 + 경기 15, 동탄·기흥·구리 포함 / 처인·화성 만세구 제외.

### 확인된 한계 (버그 아님, 공개 시 안내 필요)
- KB salePm 커버리지: 호남·강원 등은 시군구 표본이 없어 도 단위만 존재
  (예: 전주·익산 개별 없음, '전북특별자치도'만). 전국 등급표 120곳은
  "KB가 시군구 단위로 집계하는 곳 전부"라는 뜻 — 화면 문구는 이미 그렇게 표기됨.
- 광주는 2026 행정개편 반영으로 '(구)광주광역시' 계열 키 사용.
- 실거래 최근 1~2개월 과소집계(신고 기한 30일)는 기존 '알려진 한계' 유지.

## v4.0 준공후 미분양 장착 (supply_data 완성)

- 통계누리 **공사완료후 미분양현황** CSV 4개(2010.1~2026.6) 수령 → `unsoldDone` 블록 생성.
  · 스키마가 총계 파일과 다르다: `월,구분,시군구,부문,규모,호`(6열) — **부문=계·규모=계**
    행만 취하고, 이 파일은 `구분=전국·시군구=합계` 행이 이미 있어 합산하지 않는다.
  · `make_supply.py`의 `build_unsold(pattern)`이 두 형식을 모두 처리하도록 일반화
    (헤더에 "부문"이 있으면 wide 파일로 판별).
  · 결과: 257개 지역 × 198개월. 전국 최신 29,786호(전체 미분양 67,464호의 44%).
- 검증: 무작위 14표본을 원본 CSV에서 재집계해 **전부 일치**. 정합성 점검(준공후 ≤ 전체)은
  48,141개 대조점 중 52건(0.108%) 역전 — 전부 시군구·과거 시점이고 시도/전국 단위는 0건.
  두 통계의 조사 시점·집계 범위 차이에서 오는 원자료 특성으로 판단, 보정하지 않는다.
- 앱: 시군구 표에 **미분양 / 준공후** 두 열을 함께 표시(기존엔 하나만).
  준공후 비중이 50% 이상이면 붉게 강조 — 안 팔린 채 완공된 물량이 많다는 신호.
  사용법 출처 표에 "준공후 미분양" 행 추가.
- supply_data.json 184KB → 333KB.

## v4.1 "재미로 보기" 탭 (시장 공부 그룹)

흥미용 탭 — API 0회, KB 데이터 + 공개 상수만. 상단에 "정치적 평가가 아니라 기간이
겹친 숫자"라는 면책을 명시.

- **정권별 지수 변화**: 전두환(지수 시작 1986.1 클램프)~이재명(현재) 10개 정권,
  임기 시작·끝 달의 KB 매매지수 비교. 임기 길이가 달라 연평균(연복리)을 함께 표기.
  박근혜(2017.3 탄핵)·윤석열(2025.4 파면)은 실제 종료 월. 지역 셀렉트(지수 198키,
  선택기 자동 승격) + 전국 대비 막대 차트.
- **정책 연표**: 2003 10·29 ~ 2026.7 규제지역 추가까지 17건 하드코딩
  (`FUN_POLICIES`). 각 정책 시행 달 → +6/+12개월 지수 변화 표 + 지수 라인 위에
  정책 시점 산점. "효과가 아니라 그 뒤 시장" 문구로 인과·상관 구분 명시.
- **최저시급·달러 환산**: 2013~2026 고시 확정 최저시급(`FUN_WAGE`, 2026=10,320원)과
  연평균 환율 참고치(`FUN_FX`, 2026년은 진행 중이라 제외). 지역 84㎡ 가격(㎡당가×84,
  그해 12월 기준)을 필요 노동시간(연 2,080시간 환산)·달러로 표시.
  2013=100 정규화 3선 차트(아파트/시급/환율). 예: 강남 84㎡ 2013년 85.6년치 →
  2026년 146.2년치 노동.
- **강남 배율**: 강남구÷전국 ㎡당가 월별 라인. 현재 5.25배 (2013~ 최저 3.17 · 최고 5.52).
- 구현 메모: buildFun은 부팅 시점이라 DB가 없음 → 셀렉트는 `fillFunSelects()`가
  첫 렌더에서 채운다 (다른 탭과 같은 패턴). 차트는 `FUN.charts`에 보관 후 재렌더 시 destroy.

## v4.2 자산 대결 카드 (재미로 보기)

- **아파트 vs 금 vs 비트코인**: 지역 84㎡ 가격을 연말 금·BTC 시세로 환산.
  상수 `FUN_GOLD`(USD/oz 연말 종가)·`FUN_BTC`(USD 연말 종가), 원화 환산은
  `FUN_FX` 연평균 — 참고치임을 화면에 명시. 2025 확정치는 웹 검색으로 검증
  (금 $4,320/oz — 연간 +65%, BTC $87,500). 2026은 진행 중이라 제외.
- 표: 연도별 84㎡ 가격 / 금 kg 환산 / BTC 개 환산 / 원시세.
  KPI: "첫해 아파트값으로 그 자산을 샀다면 지금 몇 채" — 강남 기준
  2013→2025: 아파트 ×3.6(1채), 금 ×4.6(1.3채), BTC ×150(42채).
- 차트: 원화 기준 2013=100, **로그 축**(BTC ×150이라 일반 축은 나머지가 붕괴).
- 낙폭 리스크 면책(2018 −73%, 2022 −64%)과 단순 환산(세금·수수료 제외) 문구 포함.
- 유지보수: 매년 초 `FUN_GOLD`/`FUN_BTC`/`FUN_FX`/`FUN_WAGE`에 전년 확정치 1줄씩 추가.

## v4.3 자산 레이스 (재미로 보기)

- **총출동 비교**: 강남 84㎡(KB) · 코스피 · 코스닥 · S&P500 · 나스닥 · 삼성전자 ·
  엔비디아(시총 1위) · 금 · 비트코인 — 2013년 말=100, 로그 축, 범례 토글.
  달러 자산은 연평균 환율로 원화 환산(환차익 포함), 체크박스로 현지 통화 전환.
- **연말 종가 상수** (`FUN_KOSPI/KOSDAQ/SPX/NASDAQ/SAMSUNG/NVDA`):
  2025 확정치는 검색·기사 검증 — KOSPI 4214.17(+75.6%) · KOSDAQ 925.47 ·
  S&P 6845.50(+16.4%) · NASDAQ 23241.99(+20.4%) · 삼성전자 12만원 안착(폐장일
  신고가) · NVDA는 10/3 종가 $187.62 앵커의 $187 근사(유일한 근사치, 주석 명시).
  삼성전자·NVDA는 분할 조정가(50:1 2018 / 4:1 2021·10:1 2024).
- **결과(원화 환산, 2013→2025)**: 엔비디아 ×604 > 비트코인 ×150 > 나스닥 ×7.2 >
  S&P500 ×4.8 > 금 ×4.6 > 삼성전자 ×4.4 > 강남구 84㎡ ×3.6 > 코스피 ×2.1 > 코스닥 ×1.9.
  배당·월세·세금 제외 가격 비교임을 화면에 명시.
- **대표 단지 실거래 얹기** (온디맨드 버튼): 압구정 현대(11680·압구정동·/현대|신현대/)와
  부산 삼익비치(26500·남천동·/삼익/)를 매년 11·12월 + 최근 3개월 실거래로 조회해
  연도별 중위 평당가 선을 추가. 총 ~58회 호출(TRADE_MEMO 캐시, 재실행 0회),
  연 2건 미만·4개 연도 미만이면 "표본 부족"으로 제외. 압구정 재건축 기대 반영 주의문.
- 유지보수: 매년 상수 6종에 전년 연말 종가 1줄씩 추가 (FUN_WAGE·FX·GOLD·BTC 포함 총 10종).

## collect.html — 실거래 수집기 (v1.0, 앱 본체와 분리)

과거 실거래(2012~)를 저장본으로 쌓기 위한 일회성 도구. 저장소 루트에 두면
`/collect.html`로 접근. 앱 본체와 독립이라 index.html에 영향 없음.

- 조건: 자료 종류(매매/전월세) · 기간(YYYY-MM ~) · 시·도 칩 선택 · 동시 요청(2~8).
- 시군구 코드표 **302개 내장** — 행안부 주민등록 행정구역 코드에서 추출.
  세종(36110) 수동 추가, **강원 42xxx·전북 45xxx 개편 전 코드 별칭 포함**
  (2023.6/2024.1 이전 신고분은 옛 코드로 들어있어 함께 조회해야 누락이 없다).
- 호출은 전부 `/api/trades?kind&lawd&ym` 프록시 경유 — 인증키가 화면에 노출되지 않는다.
  동시 실행 풀 + 2회 재시도, 실패 목록 별도 보관 후 [실패분만 재시도].
- **응답 필드 자동 매핑**: 프록시 정규화형(apt/area/amount/ym)과 원본형
  (aptNm/excluUseAr/dealAmount/dealYear+dealMonth) 양쪽을 모두 인식 —
  프록시 구현이 바뀌어도 수집기는 그대로 동작.
- **연도별 자동 저장**(기본 ON): 한 해가 끝나면 CSV를 내려받고 메모리를 비운다.
  브라우저 메모리 폭주와 엑셀 104만 행 제한을 동시에 회피.
- CSV는 UTF-8 BOM. 열 구성은 실거래가 공개시스템 다운로드본에 맞춤
  (매매 12열 / 전월세 13열, API가 안 주는 도로명·매수자·등기일자는 제외).
### collect.html v1.1 — 누락 원인 대응

- **원인**: 실거래 API는 **신고 당시 법정동코드**로만 조회된다. 행정구역이 바뀐 지역을
  현재 코드로 부르면 그 시군구가 통째로 빈 결과가 된다 (군위군 경북 47720→대구 27720,
  부천 3구 폐지, 인천 남구 28170→미추홀 28177 등). 공개시스템 CSV는 과거 데이터도
  **현재 명칭으로 소급 표기**(2019년 자료에 "인천 검단구", "전남광주통합특별시")하므로
  이름 대조로는 원인을 못 찾는다 — 코드 기준으로 봐야 한다.
- **코드 탐색**: 기준 월 하나로 5자리 코드 공간(앞 2자리 × 000~999)을 훑어 그 시점에
  살아있던 코드를 전수 확인. 코드표에 없던 코드를 붉게 표시하고 [수집 대상에 추가]로
  바로 재수집. 앞자리 19개 전부면 19,000회(15~25분), 한 번만 돌리면 된다.
- **페이징**: `numOfRows=1000&pageNo=n`으로 끝까지 순회(최대 12장). 프록시가 pageNo를
  무시하면 1장과 첫 항목이 같은 것으로 감지해 즉시 중단하고 경고 로그를 남긴다.
- **자동 저장 레이스 수정**: 연도 종료 판정을 '마지막 잡 순번'이 아니라 **연도별 완료
  카운터**로 변경. 동시 실행 중이던 요청의 데이터가 빠지거나 다음 해 파일로 섞이던 문제 해결.
  저장 시에도 해당 연도 행만 골라내고 나머지는 메모리에 남긴다.
- **특정 코드만 수집**: 5자리 코드를 쉼표로 넣으면 그 코드만 조회(일회성 보충 수집용).
- **검증표 저장**: 시군구×월 건수 CSV — 공개시스템 다운로드본과 대조해 누락을 바로 찾는다.
- 과거 코드 5개 기본 내장(군위 47720 · 인천 남구 28170 · 부천 원미/소사/오정).
- 검증: jsdom 31개 항목 통과 (기존 19 + 페이징 4 + 특정코드 3 + 과거코드 5)
- 이전 검증: jsdom 19개 항목 통과 (코드표·계획 계산·호출 URL·원본형/정규화형 파싱·
  BOM·헤더 열 수·행 수·연도별 파일명·전월세 모드).

### 누락 원인 확정 (2019.09 실측 대조)

공식 다운로드본 44,078건 vs 수집기 40,483건 → **차이 3,595건, 전부 "시군구 통째 누락"**
(일부만 빠진 지역 0곳 → 페이징·잘림 문제는 없음).

- **누락 = 인천 신설 4구(제물포·영종·검단·서해) + 전남광주통합특별시 전역**(광주 5구 +
  전남 22시군). 수집 파일의 법정동코드 분포에서 **29·46 계열 0건**, 인천도
  28110/28140/28260(중·동·서구) 0건 — 즉 2026.07 개편으로 **데이터가 새 코드로 이관**됐다.
- **핵심 발견: 국토부 실거래 API는 과거 거래도 "현재" 행정구역 코드로 돌려준다.**
  같은 파일에서 강원 42·전북 45 계열이 0건이고 51·52 계열에 전량 들어있는 것으로 확인.
  → 옛 코드 별칭은 불필요(기본 OFF로 변경), 대신 **코드표가 최신 개편을 못 따라가면
  그 지역이 통째로 사라진다**. 행안부 주민등록 자료(2026.06)는 아직 옛 체계라 여기서 어긋났다.
- 대응(v1.2): 코드 탐색 기본 앞자리를 `28,29,46,53,54,55`(6,000회·5분)로 좁히고,
  [새 코드만 수집 대상에 넣기] 버튼 추가 → 탐색 직후 누락분만 재수집.

### collect.html v2.0 — 탐색 삭제, 수집 내장 자동 탐지로 전면 개편

배경: 코드 탐색(v1.2)을 앞자리 29·46·53~55·30~35 전수로 돌려도 전남광주통합특별시
코드가 안 나옴(인천 4구만 발견) — 통합 코드가 예상 밖 대역(56 이후 추정)에 있다.
앞자리를 추측하는 방식 자체가 한계 → 사용자 결정에 따라 탐색 기능 삭제, 재수집 방향.

- **코드 자동 탐지 내장**: [수집 시작]이 최초 1회 10000~99999 **전 구간**을
  `numOfRows=1` 프로브(~90,000회·40~60분)로 훑어 살아있는 코드를 찾는다.
  결과는 localStorage(`collect_codes_v1`, 기준월 202606)에 저장 — 재실행·전월세
  전환 시 즉시 생략. 앞자리 추측이 없으므로 어떤 대역이든 놓치지 않는다.
- regions() = 내장 코드표(현행 268개; 강원42·전북45 중복 제외) ∪ 탐지 코드.
  탐지 코드 중 알려진 시·도 대역은 칩 선택을 따르고, **새 대역은 무조건 포함**.
  plan()에 "탐지 N개 포함" 표시. PREFIX_SIDO 매핑(11서울~52전북).
- **번지 엑셀 날짜 깨짐 수정**: 번지를 `="1-39"` 수식 문자열로 감싸 저장(매매·전월세
  공통, excelText). 엑셀이 "Jan-39"/"07월 06일"로 바꾸던 문제 차단.
  (공식 다운로드본에도 같은 깨짐이 있음 — 우리 파일은 원본 보존.)
- **수집 후 커버리지 요약**: 시·도별 건수 로그 + 선택했는데 0건인 시·도는
  "코드 누락 가능성" 경고.
- 탐지 중 ST.abort 잔존 버그 수정(이전 중지 후 탐지가 즉시 끝나던 문제).
- 검증: test_collect2.js 28개 항목 통과 (탐지 프로브/캐시/합류/새 대역 포함,
  캐시 재사용, 번지 보호, 커버리지 경고, 특정 코드 모드, 전월세 CSV).

### 2019 재수집 검증 → 남은 문제와 해결 (collect v2.1 + 프록시 v2)

2019.08~12 공식 vs 재수집: 8월 −1 · 9월 −3 (사실상 일치) / 10~12월 −404·−2,603·−2,280.
- **신규 코드는 전부 수집돼 있었음**: 전남광주통합특별시 = **12xxx 대역**(서울 11 다음!),
  25개 코드 · 2019년 36,684건. 인천 4구(28125/155/275/290)도 수집됨. "광주 0건"으로
  보인 건 라벨이 "신규 탐지"라 이름 대조에서 빠진 것.
- **코드→지역명 확정**: 공식 파일과 (동·번지·단지) 조인, 표본 수천 건 일치로 29개 전부
  확정 → collect.html 코드표에 내장(시도 "전광"), 죽은 29·46 대역은 knownCodes에서 제외.
- **10~12월 부족의 진짜 원인 = 프록시 페이징**: 15개 시군구가 정확히 1,000건에서 잘림
  (해운대 1,891→1,000 등). 프록시가 pageNo를 무시 → **api/trades.js 교체본 작성**
  (api-trades.js: numOfRows·pageNo 통과, totalCount 응답, XML 파싱·정규화, 인코딩/디코딩
  키 자동 처리, MOLIT 오류코드 502 전달). collect v2.1은 totalCount로 정확히 페이징
  (없으면 기존 시그니처 방식 폴백).
- 남은 오차 8·9월 −1/−3(0.007%)은 API 측 정정·삭제분으로 추정 — 허용.
- 검증: 프록시 단위테스트 PROXY_OK · test_collect2 32항목 통과.
- **재수집 필요 범위**: 프록시 교체 후 전체 재수집 권장(잘림은 1,000건 초과 시군구×월
  어디서든 발생 가능, 탐지는 캐시돼 생략됨). 매매+전월세 약 2×53,000회 · 3시간 안팎.

### collect v2.2 — 매매 17개년 전수 분석 → 셀 단위 보충 수집

- 2010~2026 매매 CSV 17개(약 903만 행) 전수 분석: **월 누락 0** ·
  **잘림(정확히 1,000건) 셀 347개**. 2020년 139개 최다(패닉바잉), 2022~24는 0개(거래절벽).
  최다 코드: 27290(대구 달서 32회)·41360(남양주 31)·11350(노원 24). 2023 총행 398,100
  vs 공식 398,147(−47, 0.01%) — 잘림 외 오차는 무시 수준.
- v2.2: "셀 단위 보충 수집" 텍스트영역(`코드-년월` 쉼표) + **[매매 잘림 347셀 채우기]**
  버튼(목록 내장). 셀 모드는 기간·지역·탐지를 무시하고 해당 셀만 페이징 완주,
  결과는 `아파트_매매_실거래가_보충_연도.csv`. 병합은 변환 단계에서 내가 수행
  (원본 연도 파일의 해당 셀 행을 보충본으로 교체).
- 전월세 파일도 같은 분석이 필요 — 업로드되면 잘림 목록 추출 예정.
- 검증: test_collect2 36항목 통과.

### 부동산원 (월)행정구역별 주택거래현황 대조 — 존재 검증 완료

- R-ONE 표(현행 행정구역 243행 × 2010.01~2026.06)를 기준으로 지역×월 존재 대조.
  1차 2,991셀 의심 → 상위 시 행(수원시 등, 구 단위로 수집됨)·이관된 옛 지역
  (인천 중·동·서구→신설 4구) 제외하면 239셀. 전부 옹진·영양·괴산 같은 초소형 군.
- 결정 검증: 공식 다운로드본과 겹치는 4셀(옹진 3·괴산 1) 대조 → **공식본도 아파트 0건**.
  R-ONE은 주택 전체(단독·연립 포함)·신고일 기준이라 군 지역에서 "주택 거래는 있는데
  아파트는 0건"이 정상적으로 발생. → **지역×월 누락 없음 확정.**
- 결론: 수집본의 실제 누락은 **347 잘림 셀이 유일**. R-ONE·부동산원 월별 건수와의
  차이는 기준 차이(신고일·주택 전체 vs 계약일·아파트)라 수치 대조용으로 쓰면 안 됨.

### collect.html v2.4 — 파일 분할 저장 · 받은 파일 점검

- **엑셀 104만 행 한계 대응**: 전월세 재수집분이 연 120만 행대(2022 1,219,778 · 2023 1,279,159 ·
  2024 1,165,218 · 2025 1,168,135)라 엑셀이 "파일을 완전하게 로드하지 못했습니다"를 띄운다.
  **CSV 자체는 온전하고 엑셀 시트 한계(1,048,576행)에 걸린 것** — 재수집 불필요.
  `saveCsv`가 `#split`(기본 1,000,000, 0이면 분할 안 함) 단위로 잘라
  `아파트_전월세_실거래가_2023_1of2.csv` 형식으로 저장한다. 파트마다 헤더·BOM 포함,
  브라우저 다중 다운로드 차단을 피하려고 900ms 간격으로 내려받는다.
- **받은 파일 점검(신설)**: 대용량 CSV를 세션에 올릴 수 없어(1.5GB) 검증을 브라우저로 옮겼다.
  파일을 8MB 슬라이스로 스트리밍 파싱해 통계만 뽑는다 — 서버 전송 없음.
  · 자료 종류(매매/전월세) 헤더 자동 판별, 파일 간 혼재 경고.
  · `tailFields(line,k)`: 오른쪽에서 k개 필드만 잘라낸다. 단지명에 쉼표가 있어 따옴표로
    감싸도 뒤쪽 필드(계약년월~법정동코드)엔 쉼표가 없어 안전하고, 13M행 풀 CSV 파싱보다 빠르다.
  · `endsWithCode(s)`: 정상 행은 `,#####`로 끝난다 → 아니면 개행 품은 필드로 보고 다음 줄과 이어붙임.
  · 산출: 연도별 행수 / **월세 열 채움률·월세>0 비율**(이번 재수집의 목적) / 월 커버리지 결손 /
    정확히 1,000건 셀(실값 1,000 확인 4셀 `41150-202112·41390-202006·41465-202111·41465-202509`은 제외) /
    1,000 초과 셀 / 법정동코드 커버리지(12xxx·인천 신설 4구, 죽은 코드 29·46·42·45·28110/28140/28260 잔존).
  · [리포트 저장 JSON](수십 KB) · [셀별 건수 저장 CSV](~1MB) — **이 둘만 세션에 올리면 검증 가능.**
- 세션 측 대조용 스크립트 `verify_rent.py`도 함께 보관(원본 CSV를 올릴 수 있을 때 사용).
- 검증: `test_collect24.js` 50항목 + `test_collect24_stream.js` 13항목 = **63항목 통과**
  (분할 파트 수·파일명·행수 합계, tailFields 따옴표 내성, 화이트리스트 1,000셀, 월 결손 검출,
  죽은 코드 경고, 14MB 합성 파일 청크 경계 스트리밍, 기존 기능 회귀 7종).

### collect.html v2.5 — market_core.json 브라우저 생성기

원자료 2.5GB(매매 912만 + 전월세 1,300만 행)를 세션에 올리지 않고, **집계까지 브라우저에서 끝낸다.**
결과물 `market_core.json`(약 3~4MB)만 저장소에 올리면 된다.

- 입력: 매매 CSV(2010~2026) · 전월세 CSV(2011~2026) 각각 파일 선택. 파트 분할본도 그대로 넣으면 된다.
  **보충본은 넣지 않는다**(원본에 이미 병합돼 있어 이중 계상).
- 집계 규칙: 해제(취소) 거래 제외 · 평당가 = 거래금액 ÷ (전용 ÷ 3.3058) ·
  84㎡대 = 전용 80~88 · 59㎡대 = 55~63 · 전세 = 월세 0원 · 표본 3건 미만 달은 null.
- **시·도/전국은 개별 거래를 다시 모아 중위수**를 낸다(구별 중위의 평균이 아님). 각 월 pooled 배열을
  따로 쌓아 median. 3개월 이동 중위(`p3`)도 pooled로 계산 — 월별 중위는 거래 구성에 흔들린다.
- 스키마:
  ```jsonc
  { "v":1, "built":"2026-08-16", "m0":"201001", "months":198,
    "rule":{ "pyeong":3.3058, "band84":[80,88], "band59":[55,63], "minSample":3 },
    "stat":{ "saleRows":..., "saleCancel":..., "rentRows":..., "rentBlank":0, "bad":0 },
    "groups":{ "전국":{...}, "서울":{...} },          // 시도 17 + 전국
    "regions":{ "11680":{ "nm":"서울 강남구", "sd":"서울",
        "s":{ "o":0, "n":[], "p":[], "p3":[], "a84":[], "a59":[] },   // p=중위 평당가(만원/평)
        "r":{ "o":12, "n":[], "j":[], "d84":[], "d59":[] } } } }      // j=전세건수, d=전세 중위 보증금
  ```
  `o`는 첫 유효 월 인덱스(앞뒤 빈 구간은 잘라낸다). 전세가율은 앱에서 `d84/a84`로 계산.
- `stat.rentBlank > 0`이면 **옛 프록시 수집분이 섞인 것** — 화면에 붉게 경고한다.
- 메모리가 모자라면 매매만/전월세만 따로 돌려 두 JSON으로 저장 → 세션에서 병합(작은 파일이라 업로드 가능).
- 검증: `test_market_core.js` **60항목 통과** (밴드 경계·해제 제외·평당가 산식·중위 짝수 반올림·
  전세 판정·월세 빈칸 카운트·trimSeries 오프셋·3개월 pooled 중위·전국 pooled 재집계 대조·
  스키마 키·기존 기능 회귀). 전체 `test_collect24.js` 50 + `test_collect24_stream.js` 13 + 60 = **123항목**.


## v4.4 [핫픽스] 화성 4구 개별 코드 조회 — 과거 실거래 복구

- **[치명] 화성의 2026.1 이전 실거래가 앱 전체에서 0건이었다.** `LAWD_GROUPS`가 화성 4구를
  전부 41590으로 매핑하고 `fetchHwa`가 분구(202602) 이전 달은 41590만 조회했는데,
  **41590은 실제로 0건**이다. 단지 찾기·거래량·대장 아파트·비교·과거 검증이 모두 영향을 받았다.
- **근거(결정적)**: 매매 원자료 전수 수집(2010.01~2026.06)에서 수집기 코드표에 41590이
  들어 있어 198개월 전부 조회했는데 0건. 반면 41591 만세구 25,016건 · 41593 효행구 23,583건 ·
  41595 병점구 42,947건 · 41597 동탄구 65,956건이 **각각 198개월 전 구간**에 존재.
  → 국토부 API가 과거 거래를 **현재 행정구역 코드로 소급 재배분**한다는 확정 사실과 일치.
  같은 이유로 부천 3구(41192·41194·41196)도 전 구간 보유하고 41190은 0건,
  군위군은 대구 27720에 있고 경북 47720은 0건이다.
- 조치:
  · `LAWD_GROUPS` 화성 4구 → 41591/41593/41595/41597, "화성시"(41590) 항목 삭제.
    **부수 효과**: 4구의 value가 서로 달라져 v3.6 지역 선택기의 "value 중복" 제약이 사라졌다.
  · `fetchHwa` 삭제, `fetchTrade`의 41590 분기 삭제 (호출 1회로 감소).
  · `SUB_DISTRICT`(화성 동 목록) 비움 — 구 코드로 직접 조회하므로 불필요하고,
    목록에 빠진 동이 있으면 정상 거래를 잘라내는 부작용만 남는다.
  · K-apt 단지목록은 이관 여부가 확인되지 않아 화성 계열이면 옛 코드를 함께 조회해 병합.
  · 패치 중 `var LAWD_RESOLVED = {}` 선언이 삭제 구간에 섞여 있었는데 테스트가 잡아냄
    (앵커 삭제 시 구간 안의 다른 선언을 확인할 것).
- 검증: `test_v44.js` 26항목 통과. jsdom에서 `"use strict"` 때문에 eval의 var가 전역에
  안 붙으므로 테스트 하네스는 첫 줄의 `"use strict";`를 떼고 평가한다.

## v5.5 단지별 저장본 지연 로드 — 아파트 찾기에 "10년" 열

3층 구조의 2층을 앱에 연결했다. `trades/{lawd}.json`을 **그 지역을 볼 때 한 번만** 받아 캐시한다.

- `loadStore(code)`: fetch → 캐시(`STORE`) · 진행 중 중복 요청 합치기(`STORE_P`) ·
  404면 `false`로 기억해 다시 안 부른다. **선택 파일**이라 없어도 앱은 그대로 동작한다.
- `storeFind(st, apt, dong)`: 단지명+법정동으로 찾고, 동이 어긋나면 이름만으로 폴백.
  이름 표기 흔들림은 기존 `normName`으로 흡수.
- `storeCagr(cx, band, years)`: 최근 유효 분기 ↔ N년 전 분기로 연평균 상승률.
  그 분기가 비면 **앞뒤 2분기까지 당겨** 쓰고, 실제 기간이 목표의 60% 미만이면 포기한다.
  평형대를 지정하면 그 밴드, 아니면 **거래가 가장 많은 밴드**를 쓴다.
  한쪽이라도 거래 1건이면 `thin`으로 표시(값은 버리지 않는다 — v5.4 원칙).
- **아파트 찾기 표에 `10년` 열 신설.** 저장본에서 읽으므로 **API 호출이 0회 늘어난다.**
  표 아래 안내는 예전부터 "10년 열을 함께 보면…"이라고 말했는데 정작 그 열이 없었다 —
  이제 말과 화면이 맞는다. 얇은 표본은 `+7.2%/년~`처럼 `~`를 붙이고,
  저장본이 없는 지역은 `·`로 비운다(저장본은 있는데 그 단지가 없으면 `—`).
- 저장본은 실거래 조회와 **병렬**로 받고, 도착하면 표만 다시 그린다. 지역이 바뀌면 늦은 응답은 버린다.
  메타에 `저장본 412단지` 또는 `저장본 없음`을 표시한다.
- **크기 실측**: 이 구조의 gzip 압축률이 **22%** — 서초구 994KB → 전송 약 220KB.
  Vercel이 정적 JSON을 자동 압축하므로 지연 로드 한 번에 부담이 없다. 저장소 총량 40MB 안팎.
- 검증: `test_v50.js` **155항목** (색인·폴백·CAGR 보간·thin 판정·404 캐시·열 추가·colspan).

## v5.4 단지 단위에는 표본 하한을 두지 않는다 (원칙 확정)

**시·군·구 집계와 단지는 성격이 다르다.** 시군구 중위가는 여러 단지가 섞이므로 3건 미만이면
어느 단지 이야기인지 알 수 없어 버리는 게 맞다. 그러나 **단지는 한 건도 그 단지의 실거래다** —
버리면 정보가 사라진다. 값을 남기고 **건수를 함께 실어** 화면에서 신뢰도를 밝히는 쪽으로 통일했다.

| 대상 | 하한 | 이유 |
|---|---|---|
| market_core (시군구×월) | 3건 | 여러 단지 혼합 — 적으면 대표성이 없다 |
| trades 저장본 (단지×평형×분기) | **없음** (`medianAll`) | 건수 `c`를 함께 담아 앱이 판단 |
| 지역 밴드 백분위(120개월) | 24개월 | 분포를 만들려면 필요 |
| 대표 단지 실거래(재미로 보기) | **없음** (v5.4에서 제거) | 아래 참조 |

- v5.4 변경: `loadRepComplexes`가 `byYear[y].length >= 2`로 **연 1건인 해를 통째로 버리고
  있었다.** 압구정 현대·삼익비치처럼 거래가 드문 단지에서 연도가 통째로 사라진다.
  → 하한 제거, 연도별 건수(`RACE.cnt`)를 보관하고 "N개 연도 (그중 M개는 1건)"으로 안내.
  안내 문구도 "거래가 한 건뿐인 해도 그대로 씁니다(단지 단위는 한 건도 실거래입니다)"로 명시.
- 4개 연도 미만이면 선을 안 그리는 조건은 유지 — 점 3개로 추세선을 그리면 오독을 부른다.
- 검증: `test_v50.js` **133항목** (하한 제거·건수 보관·안내 문구 + 시군구 하한은 유지되는지).

## v5.3.1 면책 문구 수정 · 출처 표 모바일 · 고정 검증 안내 위치

- **[오류] 하단 면책에 "통계는 표본조사 기반이라"가 남아 있었다.** 실거래는 표본조사가 아니다.
  → "지역 시세는 그 달 실거래의 **중위값**이라 특정 단지·매물 가격과 다르고, 최근 1~2개월은
  신고가 덜 모여 **과소 집계**된다"로 교체. v5.3에서 본문은 고쳤는데 footer를 빠뜨렸다.
- **출처 표가 모바일에서 잘렸다.** 전역 `table{white-space:nowrap}` 때문에 4열이 화면을 넘겨
  가로 스크롤해야 읽혔다(왼쪽 자료명이 잘림). 숫자 표에는 nowrap이 맞으므로 **글이 든 표에만
  `.srctbl`**(white-space:normal · table-layout:fixed · colgroup 4열 · 720px 이하 축소)을 적용.
- **고정 검증 안내를 찾기 쉬운 곳으로.** `v5.0 이전 검증치` 표기가 Q8 한 곳에만 붙어 있어
  사용자가 확인할 방법이 없었다 → FAQ 상단 "라이브와 고정의 차이" 안내에 **고정 문항은 2026.07
  한 번 검증한 수치이고 그중 일부는 KB 자료로 검증했으며, 지금 계산 자료와 다르니 방향 참고용**
  이라고 명시.
- **패치 사고**: 문자열 구간을 잘라 교체할 때 끝 앵커가 `</div></div>'`인데 `</div>'`로 찾아
  **닫는 div 하나를 흘렸다.** `node --check`는 문자열이라 통과한다 → 테스트에 **jsdom DOM 구조
  검사**(FAQ 카드 3개·문항 17개)를 추가해 재발을 막는다.
- 검증: `test_v50.js` **128항목**.

## v5.3 KB 서술 전면 정리 (사용자에게 보이는 문구)

v5.0에서 데이터는 갈아탔지만 **화면 문구에 KB 서술이 24곳 남아 있었다.** 계산은 실거래로 하는데
설명은 KB라고 말하는 상태였다 — 사용자가 출처를 잘못 알게 되므로 전부 교체했다.

- 재미로 보기(정권별·시급 환산·자산 레이스) · 지표 사전(㎡당가) · 사용법(주의·데이터 갱신) ·
  FAQ 라이브 재계산 출처 문구 · 하단 면책 4항.
- **㎡당가 정의를 고쳐 적었다**: "KB 조사의 평균 매매가" → "실거래 전용 1㎡당 **중위** 매매가".
  평균이 아니라 중위값이라는 점이 해석에 영향을 준다.
- 표본 관련 주의문을 실거래 특성에 맞게 다시 썼다: 표본조사·표본 개편 단절 → **거래가 적은 달은
  몇 건에 흔들림(3건 미만 빈칸) · 3개월 이동중위로 지수 생성 · 재건축 이주나 특정 단지 입주처럼
  거래 구성이 바뀌면 중위가도 튄다**.
- 데이터 갱신 안내를 "KB 데이터허브 엑셀" → "**수집기(/collect.html)에서 새 달만 모아 재생성**"으로.
- **FAQ 고정 검증 13문항의 출처 표기는 남겼다.** 그 검증은 실제로 KB 자료로 한 것이므로
  지우면 거짓이 된다. 대신 `— v5.0 이전 검증치`를 붙여 현재 계산과 구분했다.
- 검증: `test_v50.js` **118항목** (UI 문구 10종 제거 확인 + 고정 검증 표기 유지 + 갱신 안내 문구).

## v5.2 착공 카드 KPI 레이아웃 수정 · 두 카드의 차이 설명

- **[버그] KPI가 그리드로 안 깔렸다.** 착공 카드의 KPI 컨테이너를 `class="stats"`로 썼는데
  **CSS에 없는 클래스**였다(이 앱은 `.status`가 2열 그리드). 그래서 위 「입주물량·수급」 카드와
  달리 stat 박스가 세로로 흘렀다. `.status`로 교체하고 `margin-top`도 맞췄다.
  → **없는 클래스명은 조용히 무시된다.** 새 카드를 만들 때 기존 카드의 컨테이너 클래스를 그대로 쓸 것.
- KPI 문구를 수급 카드와 같은 결로 정리: 최근 3년 착공 / 직전 5년 착공 / 착공 증감 /
  예상 입주 시기 / 최다 착공 해(+지금의 몇 배). **증감이 음수일 때 상승색**으로 칠한다 —
  공급이 줄면 수급은 타이트해지는 방향이라 "빨간 마이너스"는 오해를 부른다.
- **두 카드의 차이를 화면에 명시**(사용자가 실제로 헷갈린 지점):
  · 「입주물량·수급」 = 청약홈 **분양 공고** 기반. 입주예정년월이 적혀 있어 **가까운 2년이 정확**하지만
    공고가 안 난 물량(후분양·공공임대 일부)은 빠진다.
  · 「착공 → 입주 예고」 = 국토부 **착공 실적** 전수. 누락이 없고 **3년 뒤**까지 보지만
    입주 시점은 3년 일괄 적용한 근사치다.
  · 정리: **가까운 2년은 수급 카드, 3년 뒤 큰 흐름은 착공 카드.** 둘이 어긋나면 아직 공고가 안 난 물량이 있다는 뜻.
- 검증: `test_v50.js` **106항목** (KPI 컨테이너 클래스·감소 시 상승색·배수 표기·차이 설명 포함).

## v5.1.1 착공 카드가 안 보일 때 이유를 표시

배포 후 착공 카드가 화면에 안 나타남 → 원인은 저장소의 `supply_data.json`이 `starts` 블록
이전 버전(333KB)이었던 것. `renderStartCard`가 `card.style.display = "none"`으로 **조용히 숨겨**
사용자가 원인을 알 수 없었다.
- 자료가 없으면 카드를 숨기지 말고 **"supply_data.json에 starts 블록이 없다"**고 안내한다.
  차트박스만 감추고 나머지 카드(미분양·연도별)는 그대로 동작한다.
- 교훈: **선택 자료가 없을 때 UI를 통째로 숨기면 "버그"로 보인다.** 왜 비었는지 말해야 한다.
  (v3.4의 `sp-extmiss` 안내와 같은 패턴인데 신설 카드에 적용을 빠뜨렸다.)
- 검증: `test_v50.js`에 옛 파일(starts 삭제) 시나리오 5항목 추가 → **101항목 통과**.

## v5.1 착공 → 입주 예고 카드 · KB 서술 정리

### 착공 카드 (입주물량 탭 최상단)
`supply_data.starts`(2011.01~2026.06 월별·시·도별)를 연 단위로 묶어 보여준다.
- 차트: 착공 막대(골드) + 준공 막대(초록) + **착공을 3년 뒤로 민 점선**(= 그 착공이 입주로 나타날 시점).
  x축을 최근 연도 +3년까지 늘려 미래 구간을 그린다.
- KPI 5종: 최근 3년 평균 착공 / 직전 5년 평균 / 증감 / 최다 착공 해 / 예상 입주 반영 구간.
- `yearSum(blk, key)`: 월별 시리즈를 연 합으로 접되 **12개월이 다 있는 해만** 채택
  (2026 상반기처럼 반쪽인 해가 급감으로 잘못 읽히는 것을 막는다).
- 실측 결과(전국): 최근 3년 274,128호 vs 직전 5년 491,101호 → **−44.2%**.
  서울 −55.4% · 경기 −35.1%. 2015년 750,170호가 최다. → 2026~2028 입주 감소 구간.
- 주의문 명시: 리드타임은 2~4년으로 갈리며 점선은 3년 일괄 적용한 **눈금이지 예측치가 아니다**,
  재건축·리모델링 **멸실이 빠져 있어 순증과 다르다**.

### KB 서술 잔여 정리
meta description · 하단 면책 · 추천 5축 설명(급지/장기성과/수급) · 연도별 카드 범례 ·
전국 등급표 설명에서 KB 언급을 실거래 기준으로 교체. 특히 수급 축 설명을 새 산식
(미분양÷인구 40 · 착공÷인구 30 · 거래량 회전 30)으로 다시 썼다.

### 검증
`test_v50.js` **96항목** + `test_v44.js` 46항목. 착공 카드는 원본 CSV 확정치와 대조
(2015 착공 750,170 · 2023 착공 246,265 · 2025 준공 342,399)하고, 반쪽 연도(2026) 제외·
KPI 5칸·감소 판정 문구·주의문까지 확인한다.

## v5.0 KB 제거 — market_core 기반 데이터 계층 (1·2단계)

`kb_data.json` 의존을 완전히 걷어내고 국토부 실거래 집계본(`market_core.json`)으로 갈아탔다.
저작권 부담이 있는 외부 자료를 저장소에 두지 않게 됐고, 매달 엑셀→JSON 수작업도 사라졌다.

### 설계 — KB 모양의 DB를 부팅 시 합성한다
`mcToDB()`가 market_core에서 KB와 **같은 형태**(`{dates:[from,to,n], series:{key:{o,v,n}}}`)의
블록을 만들어 `DB`에 꽂는다. 덕분에 `getVal·lastVal·decVal·pmSeries·bandStats·idxChange·
fullCagr·faqCompute` 등 계산 계층을 **한 줄도 고치지 않고** 재사용한다.

| 합성 블록 | 산식 |
|---|---|
| `salePm` ㎡당 매매가 | 중위 평당가 `p` ÷ 3.3058 |
| `jeonsePm` ㎡당 전세가 | `salePm` × (`d84`/`a84`) — **같은 면적대끼리** 비교 |
| `sale` 매매지수 | `p3`(3개월 이동중위) 정규화, 첫 유효월 = 100 |
| `jeonse` 전세지수 | `jeonsePm` 정규화 |
| `vol` **신규** | 월 거래량 |
| `a84` / `a59` **신규** | 84·59㎡ 중위 실거래가 (지수가 아닌 실금액) |
| `jr` **신규** | 전세가율 % |

- 시리즈에 `n`(유효 표본 수)을 추가했다. 실거래는 중간 결측이 생길 수 있어
  `v.length`로 표본을 세면 과대 계상된다 → `sampleN()`이 이 값을 쓴다.
- KB salePm은 2013.4~(158개월)인데 **market_core는 2010.1~(198개월)**이라 오히려 40개월 길다.
  `bandStats`의 120개월 룩백·`idxChange(...,120)`·FAQ Q1/Q2/Q12/Q15가 전부 충족된다.

### 지역 키를 법정동코드로 통일
KB 고유 키(`"강남11개구|강남구"`)와 LAWD 코드 두 체계가 공존하던 것을 **코드 하나**로 합쳤다.
- `ik()` 항등 함수화, `buildLawdMap()`이 코드→코드, `NEW_DISTRICT`·`PARENT_CITY`·`KB_SIDO` 삭제.
- `disp/kbLabel/kbSido`는 market_core의 `nm`·`sd`를 그대로 읽는다.
- `RULES.regulated` 40곳 · `COORDS` **253곳** 전부 코드 기준으로 변환.
  좌표는 KB가 다루지 않던 군 지역 103곳을 시·군청 소재지 근사값으로 새로 채웠다.
- `METRO` 판정을 코드 앞 2자리(11·28·41)로 바꿨다 — v4.x는 `Object.keys` **나열 순서**에
  의존해 조용히 깨질 수 있는 구조였다.
- `RANKED`: 집계키 제외·상위시 숨김 규칙이 사라졌다(market_core는 시군구 단위만 담는다).
  **120곳 → 252곳**으로 커버리지가 두 배 이상 늘었다.

### 수급축 재정의 (KB 설문지표 대체)
`buyerIdx`(매수우위)·`jeonseSupply`(전세수급)는 KB 고유 설문이라 대체 불가 → **삭제하고
실데이터로 다시 만들었다**(`buildSupplyScores`).
- ① 미분양/인구 (40%, 낮을수록 좋음) — supply_data.unsold
- ② 최근 12개월 착공/인구 (30%, 낮을수록 좋음) — supply_data.starts, 착공→준공 2~3년의 선행지표
- ③ 거래량 회전 = 최근 12개월 ÷ 직전 5년 평균 (30%, 높을수록 수요 회복) — DB.vol
- 각 항목을 RANKED 안에서 백분위로 환산해 가중 합산 → 20~100. `SUPPLY_PARTS`에 근거 문구.
- supply_data가 없으면 ③만으로, 셋 다 없으면 60 고정.

### 그 밖
- **`pmSeries`가 null 대신 전부 결측인 배열을 돌려준다.** 전세 거래가 없는 지역은 `jeonsePm`
  시리즈 자체가 없어 호출부의 `.map`이 터졌다 (추천·전세끼고 탭 렌더 실패로 드러남).
- 재미로 보기 정권별 지수: 자료 시작(2010.1) 이전 임기는 "—"와 사유를 표시하고,
  걸쳐 있는 임기는 자료 구간만 재며 **`일부` 배지**를 붙인다.
- FAQ Q2의 지역 필터가 `k.indexOf("|")>=0`(KB 키 형태)였는데 `RANKED` 멤버 검사로 교체.
- 규제지역 편집기가 전체 이름·짧은 이름·법정동코드를 모두 받는다.
- 사용법 출처표·데이터 상태 패널의 KB 문구를 실거래로 교체.

### 패치 사고 기록
`cut(start, end)` 방식으로 `buildLawdMap`을 교체할 때 끝 앵커를 `function kbSido`로 잡았는데,
그 사이에 260줄(`subFilterOf`·`regionCell`·`buildDanji`·`buildCartTab` 등 12개 함수)이 들어 있어
**통째로 삭제됐다.** `node --check`는 통과했고 jsdom 부팅에서 `buildDanji is not defined`로 잡혔다.
→ **구간 삭제 패치는 반드시 삭제 전후 함수 목록을 diff로 확인할 것.**
(테스트에 백업 대비 함수 목록 diff를 넣어 재발을 막는다.)

### 검증
`test_v50.js` **73항목** + `test_v44.js` **46항목** = **119항목 통과**.
실제 market_core.json·supply_data.json을 fetch 목업으로 물려 부팅하고,
DB 합성값을 원본에서 직접 재계산해 대조(㎡당가·84㎡ 중위가·거래량·전세가율)했다.
17개 탭 렌더 스모크, RANKED 정렬·좌표 커버리지·규제지역·추천 5축 범위까지 포함.

### 남은 일 (v5.1)
- 입주물량 탭에 "착공 → 입주 예고" 카드 신설 (supply_data.starts 활용)
- 사용법·FAQ 본문의 KB 서술 잔여 문구 다듬기
- 저장소에서 `kb_data.json` 삭제

## v4.5 [핫픽스] 법정동코드표 실데이터 동기화 — 광주·전남 전역 복구

v4.4(화성)와 **같은 계열의 버그가 훨씬 넓게** 남아 있었다. 매매 전수 수집(2010.01~2026.06,
198개월) 결과와 앱의 `LAWD_GROUPS`를 코드 단위로 대조해 확정했다.

**죽은 코드 31개 — 조회하면 항상 0건이던 지역**
- 광주 5구 29110/29140/29155/29170/29200 → **광주 전역**
- 전남 22시군 46xxx → **전남 전역**
- 인천 중구 28110 · 동구 28140 · 서구 28260
- 인천 옹진군 28720 (아파트 거래 자체가 0건)

**코드표에 없어 고를 수조차 없던 29곳**
- 전남광주통합특별시 12xxx 25곳 · 인천 제물포 28125 · 영종 28155 · 서해 28275 · 검단 28290

즉 **27개 시군구가 실거래 탭에서 통째로 빈 결과**였고, 2026.7 개편 신설 29곳은 접근 불가였다.

- 조치: 인천 그룹에서 죽은 3구·옹진 제거 + 신설 4구 추가(10곳), 광주 그룹을 **"전남광주" 25곳**으로
  교체, 전남 그룹 삭제. 결과 **코드표 253개 = market_core 실거래 지역 253개와 1:1 일치**.
  (함평군·신안군은 12xxx 코드가 확인되지 않았고 아파트 거래도 0건이라 제외.)
- **부수 회귀 방지**: `fillKbRegionSelect`의 optgroup 순서 배열이 `LAWD_GROUPS`의 시·도만
  담고 있어, 거기 없는 시·도(KB의 광주·전남)는 **그룹째 렌더에서 빠진다.**
  order에 없는 그룹을 뒤에 붙이도록 보강했다.
- 검증: `test_v44.js` 46항목 통과 (기존 34 + 코드표 동기화 11 + 셀렉트 보강 1).
  **핵심 검사: 앱 코드표와 market_core 지역 집합이 정확히 같은지 자동 대조** — 앞으로 행정구역
  개편이 있어도 이 테스트가 잡는다.

## v4.4.1 아파트 찾기 조회 기간 표시 · v4.4 잔여 정리

- 아파트 찾기 탭은 "최근 3/6/12개월"만 고를 수 있어 **화면 어디에도 실제 기간이 없었다.**
  `DJ.ymFrom/ymTo`를 기록해 메타를 `2026.03~2026.08 · 12단지 · 매매 120건…`으로 앞에 붙였다.
  안내문에 "특정 과거 연도는 거래량(24개월)·과거 검증(5·10·15년) 탭을 쓰라"고 명시.
- v4.4에서 `HWA.guOf`를 없앴는데 `loadDanji`에 `HWA.guOf && …` 참조가 남아 있었다.
  `undefined && …`라 늘 false여서 동작에는 문제가 없었지만(법정동 필터도 비어 있어 no-op)
  죽은 분기·`DJ.usedCodeSplit` 메타와 함께 제거했다.
  **교훈: 객체 필드를 지울 때 그 필드를 읽는 곳까지 grep할 것.**
- 검증: `test_v44.js` 34항목 통과 (기존 26 + 기간 표시 8).

## market_core 커버리지 확인 (v4.4 시점)

- 253곳 = **실제 아파트 거래가 존재하는 모든 시군구(구 단위)**. 수집기 코드표 331개 중
  미등장 78개의 내역: 개편으로 죽은 코드 66개(강원42·전북45 별칭, 광주29, 전남46,
  인천 28110/28140/28260, 41590, 41190, 47720) + 12개 = 옹진군(아파트 0건) ·
  구가 있는 시의 상위 코드 11개(수원 41110·성남 41130·안양 41170·안산 41270·고양 41280·
  용인 41460·청주 43110·천안 44130·포항 47110·창원 48120·전주 52110 — 실거래는 구 단위로만 온다).
- **신규 탐지 코드는 정상 지역명으로 들어갔다.** market_core 생성기는 CSV의 시군구 라벨 열을
  읽지 않고 마지막 열의 법정동코드만 읽은 뒤 내장 코드표에서 이름을 붙인다.
  따라서 수집 CSV에 "신규 탐지 12330"으로 남아 있어도 집계에는 영향이 없다.
  12xxx 25곳 전부 "전광 ○○" · 인천 신설 4구 전부 정상.

### index.html v5.21 — '전체 평형'을 없애고, 빈 항목은 남긴다

**(1) 평형은 반드시 하나를 고른다.**
'전체 평형'은 그 단지의 모든 평형 거래를 한 줄로 세운 중위값이라, 매매와 전세의 평형 분포가
다르면 전세가율·갭이 거짓말을 한다 — 안산 주공5는 84㎡ 매매 옆에 59㎡ 전세가 앉아
전세가율이 9.1%로 찍혔다. v5.20에서 저장본 폴백만 막았는데, **실거래끼리도** 생기는 문제였다.

평형을 고정하면 매매·전세·평당가·갭이 **모두 같은 평형 안에서** 계산되어 애초에 어긋날 수가 없다.
고칠 곳을 하나씩 막는 것보다 어긋남이 생길 수 없는 구조로 바꾸는 쪽이 낫다.
대신 고를 수 있는 평형에 **135㎡대(120~)** 를 더했다 — `BANDS`에 `135:[120,9999]` 추가,
아파트 찾기·지역 1등·비교 탭 선택지에 반영.

**(2) 카드에서 빈 항목을 숨기지 않는다.**
v5.18은 값 없는 칸(`—`)을 `display:none`으로 접어 카드를 짧게 만들었는데,
**항목까지 사라지면 자료가 없는 건지 칸이 없는 건지 헷갈린다.**
항목은 그대로 두고 `opacity:.45`로 흐리게만 한다.

**(3)** 옛 전세로 비운 전세가율·갭 칸에 `jeTip()`으로 이유를 단다 — 빈칸만 있으면 고장으로 오해한다.

### index.html v5.23 — 마법사 사용 피드백 12건

  1. 관심지역: 시·도를 고르면 시·군·구가 **칩으로 펼쳐져 여러 개를 한 번에 토글**(`renderWizGu`).
     칩 클릭 리스너는 `onclick` 교체 — addEventListener를 쌓으면 한 번 눌러도 여러 번 토글된다.
  2. 광역권을 ~도 표기로, `.pickchips.grid3`(3열 격자)로 좌우 꽉 차게 (2줄).
  3. 기간 버튼 `.grid2` 2열 격자.
  4. 평형에 **소형(46㎡ 이하)** — `wizBands()`가 36·46 두 밴드를 함께 훑고 단지당 나은 쪽 하나만.
  5. 예산 직접 입력(억) — 입력하면 칩 선택이 풀린다.
  6. 결과 카드에 같은 평형대 **전세(연도)**, 매매와 같은 해면 **갭**까지. 다른 해면 갭은 안 만든다(v5.20 원칙).
  7. '왜 추천했나요?' 근거마다 검증 화면 링크 — 성과→아파트 비교, 급지→지역 채점표·전국 등급표,
     거래→거래량, 조건→돈 계산. 저관심자는 안 눌러도 되고, 고관심자는 눌러서 파고든다.
  8. **버그**: [이 지역 단지 더 보기]가 값을 넣고 showTab을 불렀는데, showTab("danji")가
     `renderDanjiShell → fillRegionSelects`로 지역 select를 **다시 채워 값을 지웠다.**
     탭을 먼저 열고 값을 넣도록 순서 교정.
  9. 결과 하단을 '투자 로드맵'(옛 사용법, 탭 이름 변경) 이동 버튼으로 교체.
 10. 모바일 카드(carded) 스타일 다듬기 — 그림자·둥근 모서리·여백 (전체 채점표 포함 전역).
 11. **버그**: cardify가 사용법의 자료 표(.srctbl)까지 카드로 접어 고정 레이아웃이 깨졌다 → srctbl 제외.
 12. 뒤로가기 = 이전 화면 — showTab이 history에 `{tab}`을 push하고(같은 탭 중복 방지, 첫 탭은 replace)
     popstate로 복귀. `NAV_POP` 플래그로 복귀 시 다시 쌓지 않는다.

### index.html v5.22 — 메인 기능을 '추천받기' 마법사로

방향 한 줄: **"초보자는 한 번의 추천으로 답을 얻고, 숙련 투자자는 그 뒤의 데이터를 깊게 파고든다."**

**구조** — 앱을 열면 4문항 마법사(`wiz` 탭)가 먼저다.
```
① 지역   관심지역 직접 선택(복수) ⟷ 잘 모르면 광역권(수도권·강원·충청·전라·경상·제주)
② 기간   단기 3~5년(5년 성과로 평가) / 장기 10년
③ 연식   신축(≤10) / 준신축(10~20) / 구축(>20) / 상관없음  + 평형(59/84/101)
④ 예산   3·5·7·10·15억 / 상관없음 (매매가 기준)
→ 단지 TOP 5
```
기존 '추천 받기'(전국 5축 채점)는 **'어디를 살까' 그룹의 '지역 채점표'**로 이동 — 상세·설득 자료 역할.

**계산은 전부 저장본에서** — `wizRun()`이 지역별 `trades/`를 열어 단지를 채점하고,
결과 5곳만 `kapt/`에서 세대수를 붙인다. **실거래 API 0회**, 결과가 1초 안에 나온다.
  - 후보 지역: 직접 선택은 그대로(≤10), 광역권은 RANKED(급지) 순으로 지역 84㎡ 중위가가
    예산×1.25 이하인 12곳 (`wizRegions`)
  - 단지 필터: 최근 2년 거래 있음 · 표본 5건 이상 · 연식 · 예산(연 중위가 ≤ 예산)
  - 점수: 기간 CAGR(만점 연 7.2%) 50% + 급지 백분위 30% + 거래 꾸준함(17년 건수) 20%
  - 지역당 최대 2곳으로 다양성 확보. 저장본 없는 지역은 개수로 알린다.

**단계적으로 깊어지는 결과** — 카드마다
  - `왜 추천했나요?` 펼침: 성과·급지·거래·조건 네 가지 근거를 숫자로
  - `이 지역 단지 더 보기`: 아파트 찾기 탭으로 이동 + 지역·평형 자동 설정 + 자동 조회
  - 하단에 `지역 채점표`·`내 조건` 이동 버튼. **다른 화면을 다녀와도 WIZ 상태가 남아** 이어진다.

주의점(테스트로 배움): 부팅(DOMContentLoaded)은 비동기라 `wizRun`이 그 전에 불리면
`#wiz-result`가 없다 — 가드 추가. RANKED가 1곳이면 급지 백분위가 0으로 나눠 NaN — 가드 추가.

### index.html v5.20 — 시점이 섞인 값은 아예 비운다

안산 주공5에서 이렇게 나왔다.
```
매매 4.95억 (2026) · 전세 4,500만 (2025 저장본) · 전세가율 9.1% · 갭 4.5억
```
전세가율과 갭은 **다른 해의 두 값을 나눈 것**이라 숫자로서 뜻이 없다.
흐리게 칠하고 연도를 붙여 뒀지만 그 정도로는 부족하다 — 표에 숫자가 있으면 사람은 읽는다.

  - 저장본에서 끌어온 전세로는 **전세가율·갭을 계산하지 않는다**(`—`). 전세 금액만 연도와 함께 보여준다.
  - **전체 평형**일 때는 폴백 자체를 하지 않는다. 저장본은 평형대별로 나뉘어 있어 어느 평형의
    전세인지 고를 수 없고, 매매(전체 평형 중위)와 평형이 어긋난 채 붙는다.
    주공5의 4,500만이 딱 그 경우였다 — 소형 전세가 84㎡ 매매 옆에 앉았다.
  - 폴백은 `sj.band === band`일 때만 채택한다.

원칙: **참고하기 애매한 값은 흐리게 칠하는 대신 비운다.**
v5.15에서 근거 없는 '역세권'을 걷어낸 것과 같은 판단이다.

### index.html v5.19 — 정렬 칩이 안 보이던 CSS 순서 실수

v5.18에서 정렬 칩을 넣었는데 휴대폰에서 안 보였다. 규칙을 이 순서로 적은 게 원인:
```css
@media (max-width:720px){ ... .sortchips{display:flex} }
.sortchips{display:none}          /* ← 뒤에 있어서 이쪽이 이긴다 */
```
우선순위가 같으면 **뒤에 온 쪽이 이긴다.** 미디어 쿼리는 특이도를 올려 주지 않는다.
기본 규칙을 앞으로 옮기고 `@media (max-width:720px){ .sortchips{display:flex} }`를 뒤에 뒀다.
같은 실수를 다시 하지 않도록 두 규칙의 **위치 순서**를 시험에 넣었다(test_cards.js).

### index.html v5.44 — 점검 보완: 미니 진행 표시 · 비아파트 태그 확대

**속도 점검(v5.43이 느려졌나?)** — 오버레이는 네트워크 호출 0회(DOM 갱신뿐), v5.42의
세대수 검증은 지역당 matchKaptAll ~50ms(12지역 최악 0.6초, jsdom 기준 — 실제 브라우저는
더 빠름) + kapt JSON 로드는 trade 저장본과 **병렬**이라 체감 추가는 미미. 파일 +14KB.
"더 느려진 느낌"의 실체는 **추천 결과가 이미 다 보이는데 오버레이가 화면을 덮어**
기다림이 강제된 것 — 아래 ①로 해소.

**① 미니 진행 표시** — progOpen(제목, 총량, mini). mini는 딤 없이 하단에 작은 진행
카드만(#progbox.mini, pointer-events 통과) — 추천받기 시세표 채우기에 적용. 카드는 즉시
읽고, 표는 차오르는 대로 채워진다. 결과 자체가 없는 로딩(아파트 찾기·대장 목록 등)은
종전대로 중앙 오버레이.

**② 비아파트 유형 태그 확대** — `nonAptType(nm)` 공용 헬퍼(주상복합·오피스텔·
도시형생활주택·생활숙박시설·레지던스). 추천받기는 후보 제외(종전), **아파트 찾기·대장
목록은 전수 조사가 목적이라 빼지 않고 단지명 옆 경고 태그**, 최종 검토 카드엔 검토 메모
경고("채점표 공식이 잘 맞지 않음 · 환금성·대출 조건 별도 확인"). "추천엔 없는데 찾기엔
있다"의 어긋남을 설명으로 잇는다.

시험: test_cards.js [3-7] — mini 클래스 토글·CSS, nonAptType 5종, 세 화면 반영(소스).
총 1,190개 통과.

### index.html v5.43 — 화면 중앙 진행 표시 (%·막대·카운트)

버튼 속 "(0/48)" 카운터와 표의 — 자리 표시는 눈에 안 띈다는 사용 소감. 여러 번 호출하는
로더 전부에 **화면 중앙 오버레이**(#progbox — 퍼센트 + 채워지는 금색 막대 + n/전체)를
띄우고, 완료되면 내려 채워진 결과가 보인다.

- `progOpen(제목, 총량)` / `progTick()` / `progClose()` — 재사용 컴포넌트. 총량 0이면
  무한 흐름 막대(indet 애니메이션, 호출 수만 표시) — 백테스트처럼 총량 미정인 로더용.
- **백그라운드로** 버튼 — 오버레이만 내리고 로딩은 계속(버튼 카운터가 그대로 남아
  진행을 잃지 않는다). 닫힌 뒤의 progTick은 무시 — 잔여 콜백이 새 오버레이를 오염 못 한다.
- 연결한 로더 8곳: 추천받기 시세표(wizLoadDetails, 지역당 12회) · 아파트 찾기 ·
  대장 목록 · 거래량 · 입주물량(전국) · 단지 비교(목록 만들기·실거래 조회) · FAQ 백테스트.
  실패(catch) 경로에서도 progClose를 먼저 불러 alert 전에 내린다.

시험: test_cards.js [3-6] — %·막대·카운트, 총량 초과 방지, 백그라운드로, 무한 막대,
닫힌 뒤 tick 무시, 로더 8곳 연결(소스 확인). 총 1,178개 통과.

### index.html v5.42 — 추천받기 보완: 소규모 신축·주상복합이 상단을 차지하던 문제

**사용 사례(실데이터 검증)** — 수도권·신축·59㎡·8억 이하에서 1·2위가 응암동
'명남스카이뷰'·'아르지움주상복합'(각 92점). 배포된 kapt/11380.json을 확인한 결과 **둘 다
K-apt(의무관리대상)에 없는 소규모 건물** — 이력이 짧은 신축이라 지역 10년 성과(은평
+8.2%/년)를 그대로 빌려 받았고, 비싼 값이 대장성 보정까지 얹어 최상단에 올랐다.
사용자 지적대로 주상복합·오피스텔은 아파트 상승 공식이 통하지 않는 별개 시장이다.

**① 비아파트 이름 제외** — `NON_APT_RE`(주상복합·오피스텔·도시형·생활주택·생숙·레지던스)가
이름에 들면 후보에서 뺀다. 제외 수는 `WIZ.out.nonApt`로 결과 상단에 표기. 내 집 채점(only)은
기준선이므로 유형과 무관하게 거르지 않는다.

**② 신축 '지역 성과 대여'에 규모 검증(FAQ Q3)** — 지역 kapt 저장본을 wizRun에서 채점 전에
싣고(matchKaptAll로 후보 전체에 hh 부착), perfSrc=region(이력 5년 미만) 신축은:
300세대 미만 → 빌린 성과 **50점 상한**(q3="small") · K-apt에 없음 → **40점 상한**(q3="unk").
q3 단지는 **대장성 2차 보정도 미적용** — 비싼 나홀로 신축이 '대장'으로 읽히는 것을 막는다.
kapt 저장본이 없는 지역은 검증 불가라 종전대로(일괄 감점 없음). 자기 이력 5년+ 단지는
제 기록 우선. 카드 칩("세대수 미확인 — 지역 성과 미적용" 등)과 '왜 추천했나요?' 성과 줄에
상한 사실을 그대로 적는다.

**③ 내 집 채점 보정** — 종전엔 내 집 평형이 마법사 평형과 다르면 채점이 실패해 **0점
고정 폴백**이 됐고, 모든 후보가 '동급 교체 · 점수 +92' 같은 배지를 달았다.
내 집 채점은 `anyBand`(전 평형 스캔)로 고치고, 그래도 실패하면 `score:null` — 점수 비교
(동급 교체)는 끄고 가격 비교(상급 이동)만 남긴다. 0점 표기·배지 문구도 null 가드.

시험: test_wiz.js [5-7] 이름 제외(집계·내 집 예외), [5-8] 규모 검증(50/40 상한·대장성 미대여·
kapt 없는 지역 예외·자기 이력 우선·비싼 나홀로 역전 방지), [5-9] anyBand·score null 가드.
총 1,162개 통과.

### index.html v5.41 — 선택창 구글지도 아이콘: 핀 → G 글자 (사용 소감)

네이버 N · TMAP T와 같은 글자 방식이라 한눈에 구분된다. 구글 파랑(#4285f4).

### index.html v5.40 — 위치 버튼: 지도 앱 선택창

위치를 카카오맵 하나로 직행하지 않고, 아래에서 올라오는 **선택창**(#mapsheet)에서
**네이버지도 · 카카오맵 · 구글지도 · TMAP** 중 쓰는 앱을 고른다 (사용 요청 — 축구팀 앱 예시).

- `mapAppUrl(id, q)` — 전부 검색 딥링크: naver `map.naver.com/p/search/`, google
  `google.com/maps/search/?api=1&query=`, tmap `tmap://search?name=`(웹이 없어 스킴 —
  휴대폰에 앱 필요, 선택창에 안내), kakao `map.kakao.com/link/search/`(기본값).
- `openMapSheet(q, title)` — 시트를 한 번만 만들어 재사용, 제목은 단지명(생략 시 검색어).
  딤·✕로 닫고, 앱을 고르면 새 탭으로 열고 닫힌다. `openKakaoMap`은 카카오 직행으로 유지.
- 공식 로고는 상표라 못 쓴다 — 브랜드색 배경(#03c75a·#fee500 등) + 일반 핀·글자만.
- 부르는 곳 네 군데(dj·fg·최종 검토·추천받기)는 openMapSheet로 교체.

시험: test_cards.js [3-5] — 앱별 URL 4종, 시트 생성·제목·버튼 4개, 네이버 클릭 → URL·닫힘,
재열기. 총 1,142개 통과.

### index.html v5.39 — 아이콘 다듬기 (사용 소감)

- 빼기: × → **−(마이너스)** — 담기가 +니까 짝이 맞는다.
- 위치: 핀 → **접힌 지도** — 카카오맵 공식 로고는 상표라 쓸 수 없고, 지도 그림이
  "카카오맵으로 연다"는 뜻이 핀보다 잘 읽힌다.
- 키 이름(pin·del)은 유지, `BICONS`의 그림(path)만 교체 — 부르는 곳 여덟 군데 무변경.

### index.html v5.38 — 위치 버튼(카카오맵) · 버튼 아이콘 · 표 유지(keeptbl)

**① 위치 버튼** — 단지 정보가 보이는 네 곳(아파트 찾기 표 `data-djloc` · 대장 목록 표
`data-fgloc` · 최종 검토 카드 `data-cloc` · 추천받기 카드 `data-wzloc`)에 위치 버튼.
단지 좌표 자료가 없으므로 `openKakaoMap(q)`가 **카카오맵 검색 딥링크**
`https://map.kakao.com/link/search/<지역 동 단지명>`을 새 탭으로 연다 — 휴대폰에선 카카오맵 앱,
PC에선 웹 지도. (COORDS는 시·군·구 단위뿐이라 지도 탭의 핀과는 별개 경로다.)

**② 버튼 아이콘** — `BICONS`(pin·plus·check·del) + `bicon(name)`이 13px 인라인 SVG
(`stroke:currentColor`)를 만든다. 담기=+, 담김=✓(두 곳의 `textContent="담김"`을
`innerHTML=bicon("check")+"담김"`으로 교체), 위치=핀, 빼기=×.

**③ 표 유지(keeptbl)** — 대장 목록·아파트 찾기 표는 휴대폰에서 카드로 접는 것보다
**가로 스크롤 표가 비교하기 편하다**는 사용 소감. `#dj-table`·`#fg-table`에 `keeptbl` 클래스를
달고 cardify가 `.srctbl`처럼 건너뛴다 — carded가 안 붙으니 720px 미디어 쿼리도 무효, `.tblwrap`의
`overflow-x:auto`가 가로 스크롤을 준다. 정렬 칩은 cardify와 무관하게 계속 동작. 아파트 찾기의
안내문("카드로 접히고" → "옆으로 밀어 보세요")도 실제 동작에 맞췄다. 다른 표들(급지표·갭투자
등)은 종전대로 카드로 접힌다 — 이 둘만 사용자가 표로 쓰고 싶다고 한 화면이다.

시험: test_cards.js [3-3] keeptbl 건너뛰기(+두 표의 클래스·overflow CSS 소스 확인),
[3-4] openKakaoMap URL(공백 정리)·bicon SVG·네 곳의 위치 버튼·아이콘 CSS. 총 1,130개 통과.

### index.html v5.37 — 최종 검토 카드도 본문이 카드 전체 폭

지역 채점표 카드는 v5.32에서 본문을 `.rcfull`로 빼내 전체 폭을 쓰게 했는데, 같은 `.reco`를
쓰는 **최종 검토(장바구니) 카드는 손대지 않아** 칩·레이더·환경·검토 메모가 점수 칸 옆의
좁은 열에 갇혀 있었다(칩이 6줄로 접히고 메모가 한 줄에 서너 글자). 같은 구조로 맞춘다.

- `.body`는 단지명·지역·연식·세대수만, 나머지는 전부 `.rcfull`로.
- 등급 뱃지(`.grade`)도 첫 줄 맨 앞(order 0) — 지역 카드의 번호와 같은 자리.
- 결과: **첫 줄 [등급][단지명][점수·필요현금·빼기], 그 아래로 칩·레이더·메모가 카드 가로 전체.**

### index.html v5.36 — 카드 배치 확정 · '내 집보다 좋은 곳'의 정의 확장

**② 배치** — v5.35에서 철회한 `.reco .body{flex:1 1 auto}`는 **전체 폭을 만드는 규칙이 아니었다**.
전체 폭은 `.rcfull`·`.attbox`의 `flex-basis:100%`가 담당하고 계속 살아 있다(그 한 줄은 제목 칸의
폭 계산만 바꿔 장바구니 카드에서 money를 다음 줄로 밀어냈다). 이번엔 기본값에 기대지 않고
order를 전부 못 박았다 — `no:0 · body:1 · money:2 · rcfull:3 · attbox:4`.
결과: **첫 줄 [번호][제목][점수·등급], 그 아래로 필요현금·레이더·근거·버튼·행렬이 카드 전체 폭.**

**④ '지금보다 나은 곳' 두 갈래** — v5.35의 `price > 내 집`은 "비싸면 좋다"는 뜻이라 좁았다.
사용자 정의대로 둘로 나눴다.
- **상급 이동(up)** — 내 집보다 비싸면서(+5% 초과) **감당 가능한 금액 안**.
  갈아타기는 `내 집 시세 + 동원 현금`, 추가매수는 `maxPriceForCash`(현금·대출)로 상한을 잡는다.
- **동급 교체(swap)** — 내 집과 **±15% 안의 값**인데 **종합 점수가 더 높은** 단지.
  같은 돈으로 더 나은 물건으로 바꾸는 경우다.
이를 위해 내 집도 후보와 **같은 축으로 채점**한다 — `wizScanStore(..., {only, noAge, noBudget})`로
내 집 한 곳만 조건 필터 없이 돌려 `HOME_REF{price, score}`를 만든다. 필터는 대장성 2차 보정이
끝난 뒤(wizRun)에 적용해 점수 비교가 정확하다. 카드 배지는 갈래별로
"상급 이동 +X억" / "동급 교체 · 점수 +N", 결과 상단과 빈손 안내에도 기준을 적는다.

### index.html v5.35 — 사용 피드백 5건

**①** '왜 추천했나요'의 [전세가율 밴드]·[입주 물량] 버튼이 화면만 열고 조건을 안 넣었다
(wizGo에 gap·supply 갈래가 없어 그냥 showTab만 됐다) → 전세는 `GAP.region` 주입 후 renderGap,
입주물량은 그 단지의 **시·도**를 골라 [불러오기]까지 누른다.

**②** 최종 검토 카드에서 점수·필요현금이 맨 아래로 밀린 것은 v5.32에서 넣은
`.reco .body{flex:1 1 auto}` 때문 — 지역 채점표 카드만 보고 넣었는데 `.reco`를 공유하는
장바구니 카드에서 본문이 한 줄을 다 먹고 money가 다음 줄로 넘어갔다 → 규칙 철회.

**③ 로드맵 무한 반복** — 추천받기 → 로드맵 → 추천받기로 도는 고리. →
`tourStepDone()`으로 단계 완료를 판정해 ✓·"완료" 배지를 달고, **[이어서 하기]**가 첫 미완료
단계로 보낸다. 마법사 하단 버튼은 "투자 로드맵으로" → **"다음 단계로"**: 안내 진행 중이면
로드맵을 거치지 않고 그 코스의 다음 단계로 직행한다.

**④ 무주택/유주택 차이 + 갈아타기 기준선** — 코스 카드에 "이 코스만의 것"을 한 줄로 명시
(무주택: 사다리·등급표·입주물량·전세 끼고 / 갈아타기: 배율·다음 오를 곳·국면·자금, 타이밍
지표가 배율 밴드로 바뀜). 그리고 내 조건에 **지금 사는 아파트**(`f-homeapt`, 내 지역 저장본에서
목록을 채움)를 추가해, 갈아타기·추가매수면 마법사가 **내 집보다 비싼 단지만** 추천하고
카드에 "내 집 +X억"을 표시한다(`wizHomeBase`).

**⑤ 점수가 다 80점대** — 조건을 통과한 후보들 사이의 **상대 평가**라 상위 5곳은 원래 높다.
판단 기준이 되도록 후보 전체 분포(최고·중간·최저)를 결과 상단에 적고, 카드 점수 아래를
"종합" → **"상위 N%"**로 바꿨으며, 근거에 '점수' 줄을 더해 상대 위치임을 밝힌다.

### index.html v5.34 — 추천받기·투자 로드맵을 다른 탭·FAQ와 정합시킴

`audit_coverage.js`로 "각 탭이 만든 지표가 추천받기·로드맵에 실제로 들어가는가"를 감사했다.
결론: **추천받기는 급지·단지 과거상승률·거래건수 3개만 썼고, 로드맵은 100% 정적**이었다.
게다가 FAQ가 자기 백테스트로 부정한 것을 코드가 그대로 하고 있었다.

**FAQ와 코드가 어긋나던 것 — 고침**
- **Q17** "과거에 많이 오른 곳 가점은 거꾸로 작동해 **배점에서 뺐습니다**" ↔ 실제 `wPerf: 25`
  (급지 다음 최대), 마법사는 단지 CAGR에 **50%**. → 채점표 배점을 Q17 결론대로
  **급지 45 · 타이밍 20 · 자금 15 · 성과 10 · 수급 10**으로 재배분(`wVer`로 기존 저장값 이전),
  마법사도 **급지 .45 · 성과 .25 · 거래 .15 · 타이밍 .15**로. 성과는 통과선(연 7.2%) 확인용.
- **Q17** "수급은 예측력이 약해 비중을 낮췄다" ↔ 수급 15 = 타이밍 15 → 수급 10 < 타이밍 20.
- **Q3** "300세대를 최소 조건으로" ↔ 마법사가 세대수를 안 봄 → 결과 카드에 **300세대 미만 배지**,
  근거에 세대수와 Q3 검증 인용.
- **Q4** "수도권은 1등보다 중간(−13%p), 지방은 1등(+28%p)" ↔ v5.28 신축 대장성이 전국 동일 →
  **수도권 0.15 / 지방 0.4**로 차등.
- **Q5·Q14** 평형·연식 선택 근거가 화면에 없음 → 3단계에 검증 결과 안내 추가.

**탭 연결이 끊겨 있던 것 — 이음**
- 마법사에 **타이밍 축**(`wizRegionTiming`) 추가 — 지역 채점표와 **같은 지표**(무주택=전세가율 밴드,
  유주택=내 지역 대비 배율 밴드)를 쓰고 40~100으로 좁혀 급지 안에서 순서만 조정(Q17 ②).
- '왜 추천했나요'에 **타이밍 줄** 신설 + 전세가율 밴드·입주물량 탭 링크 → 검증 링크 5개 → 7개.
- **투자 로드맵**: TOURS에 **추천받기가 아예 없었고** reco를 "추천 받기"로 잘못 부르고 있었다 →
  wiz를 2단계에 넣고 이름 정정, 무주택 코스에 전국 등급표·입주물량, 갈아타기 코스에 전세 끼고를
  더해 핵심 13개 탭을 모두 안내. 맨 위에 **'지금 내 상황'** 카드(`renderGuideMine`)를 새로 만들어
  내 조건·최대 매수가·추천 TOP3·그 지역들의 5축과 필요현금을 **실제 값으로** 채운다.

### index.html v5.33 — 연식×급 '검증 방법' 감사 + 교정 4건

`audit_tier.js`(앱 함수를 그대로 불러 경계·편향을 실측하는 감사 스크립트)를 만들어
신축/준신축/구축 각각의 산출 방법을 항목별로 확인했다.

**이상 없던 것**
- 연식 경계: ≤10 신축 / 11~20 준신축 / 21~ 구축 — 마법사 `wizAgeOk`와 정확히 일치.
- 대장/중위/하위 인덱스: n=1·2·3·4·9·40에서 각각 #0 / 중앙 / 하위 25% 지점으로 정확.
  (n=1·2일 때 같은 단지가 중복되면 dedup되어 줄이 줄어드는 것도 의도대로)
- 표본 가드(2건 이상인 최근 해로 물러남): 정상.
- 연식 미상 단지 제외: 정상.

**교정한 것**
- **D. 서로 다른 해의 값으로 순위를 매기던 문제** — 2024년 8.0억이 2026년 8.5억보다
  아래로 갔다. 그 사이 오른 시장이 통째로 무시되는 셈이고, 최신 연도 값이 남기 쉬운
  신축과 오래된 값이 남기 쉬운 구축 사이에 계통 편향을 만든다. →
  `tierYearAdj()`(지역 매매지수 비율)로 **현재 시점 환산값(padj)으로 줄을 세우고**,
  표시 금액은 실제 거래된 그 해 중위가를 그대로 둔다. 지수가 없거나 배수가
  0.5~2배를 벗어나면 환산하지 않는다(조용한 무해 동작).
- **E. 1년 전 전세로 갭을 만들던 문제** — "다른 해 매매·전세로 갭을 만들지 않는다"는
  v5.20 원칙을 이 표만 어기고 있었다(`jy < yr - 1`) → `jy !== yr`이면 갭 없음.
- **A. 라벨이 경계와 1년 어긋남** — "준신축 10~20년"인데 10년차는 신축, "구축 20년~"인데
  20년차는 준신축이었다 → "신축 10년 이하 / 준신축 11~20년 / 구축 21년 이상"
  (마법사 연식 칩 문구도 같은 기준으로 통일).
- **F·I. 말 없이 사라지던 정보** — 그룹이 1~2곳이면 중위·하위 줄이 조용히 없어졌다 →
  "N곳뿐이라 대장만 표시"로 밝히고, 곳수 tooltip에 "기준 면적 거래가 있는 단지 수 —
  이 면적대 거래가 없는 단지는 빠집니다"를 명시. 캡션에 **순위 방법**(그룹 안에서의
  순위 · 시점 환산 · 같은 해 갭)을 적어 뒀다.

### index.html v5.32 — 카드 전체 폭 + 실데이터 검증 교정 3건

**① 레이아웃** — 지역 채점표 카드에서 필요현금 줄·레이더·추천 근거·버튼을 `.rcfull`
(flex-basis:100%, order:3)로 묶어 **카드 전체 폭** 사용. `.body`엔 제목만 남는다.
행렬(.attbox)은 order:4로 그 아래. 폭이 넉넉해졌으니 단지정보의 매매·필요현금은 **두 줄로 복귀**.

**② 실데이터 검증** — 팔달구 카드의 모든 수치를 market_core/trades 원본으로 재계산해 대조:
서열 29위·2분위·1,009만/㎡·10년 +165.7%·연복리 10.3%(p3 1245→3308)·84㎡ 중위 6.75억
(a84 최근 3개월 67500/66500/73800)·필요현금 4.23억·대출 2.7억·전세가율 62%(d84 45500/a84 73800)·
5축 가중합 85.2 — **전부 일치**. 단지 3곳의 필요현금(9.3→5.95 / 6.9→4.33 / 5.88→3.65)도 재현.
그 과정에서 문제 3건 발견·수정:

- **타이밍 근거 불일치(버그)** — 유주택이면 점수는 *배율 밴드*로 매기는데(칩 "배율 84%", 50점)
  근거 글은 언제나 *전세가율 밴드*(28%)를 설명했다. 서로 다른 지표를 같은 화면에 쓰던 셈.
  → `scoreRegion`이 `timingMode`/`timingPct`를 돌려주고, 근거는 **실제 쓴 지표**로 쓰되
  반대 지표를 괄호 참고로 덧붙인다.
- **1건짜리 중위가가 대표로(데이터 품질)** — 연식×급 행렬에 표본 가드가 없어
  수원성중흥S-클래스 6.9억(2026년 **1건**), 인계극동스타클래스 4.45억(2026년 **1건**)이
  '중위' 자리에 올라왔다. 마법사는 cnt≥5인데 여기만 무방비였다.
  → **거래 2건 이상인 가장 최근 해**를 쓰고, 없으면 1건이라도 쓰되 **건수를 항상 표기**.
- **㎡당가 vs 기준 면적 매수가 괴리** — 1,009만/㎡ × 84 = 8.5억인데 매수가는 6.75억.
  ㎡당가는 전 평형 혼합 중위(소형이 ㎡당 비싸 높게 잡힘)라 그렇다. 오해를 없애려
  제목 줄에 **"84㎡ 중위 6.75억"**을 병기하고 ㎡당가에 설명 tooltip.

### index.html v5.31 — 분위 다중 선택 · 제자리 설명 · 행렬 전체 폭

**①** 등급표 "1~3분위만" 체크 → **전체·1·2·3·4분위 다중 선택 칩**(`TIERS.dec`,
비어 있으면 전체 · 전체 클릭 시 초기화). 그리고 휴대폰 카드에서 **항목 값을 누르면
그 자리에서 설명 토글**(`.cellhelp`) — 위 칩까지 스크롤했다 돌아오면 보던 지역을 잃는 문제.

**②** 연식×급 행렬을 `.reco`의 직계 자식으로 옮겨 **카드 전체 폭** 사용
(flex-wrap 줄바꿈, `.reco>.attbox{flex-basis:100%}`). 폭이 넉넉해져
매매·필요현금은 한 줄로 복귀.

**③** 거래량 이동평균의 `*` 표식 제거 — 값이 정확한데 낯선 기호가 오히려 의문을 만든다.
설명은 주석 문장으로만.

**④** 채점표 [갈아타기 분석]: 목표 지역 select를 옵션 훑어 맞추고 change까지 쏜 뒤
**맨 위로 스크롤** — 이전 스크롤 위치에 남으면 딴 지역 자료처럼 보였다. [단지 보기]도 맨 위로.

### index.html v5.30 — 등급표 안내 휴대폰 대응 · 단지명 줄바꿈

**①** v5.29의 등급표 머리글 클릭 안내가 휴대폰에서 안 나왔다 — 카드 모드에선
thead가 `display:none`이라 누를 곳 자체가 없다. → 표 위에 **항목 설명 칩**
(`#tr-helpchips`, sortchips와 같은 "좁은 화면에서만 표시" 방식)을 추가하고
설명 토글을 `trHelpToggle`로 공용화. 84/59㎡ 매매는 칩에서 "매매" 하나로 합침.

**②** 연식×급 블록 단지명 잘림 — 말줄임(nowrap+ellipsis) 대신 줄바꿈 허용(word-break).

**③(질문 답)** 거래량 12개월 평균은 저장 방식 변경이 필요 없다 — 연별 저장본(trades/)은
단지 시세용이고, 거래량 이어붙임은 **이미 월별인 market_core**를 쓴다(v5.29).

### index.html v5.29 — 사용 피드백 4건 (라벨 오류 교정 · 이동평균 이어붙임)

**①** 연식×급 블록에서 매매·필요현금을 각각 한 줄로 — "(대출 · 가능)"이 줄을 넘던 문제.

**② '최근 실거래' 열 라벨 오류(중요)** — 아파트 찾기 표의 그 열은 실제로는 조회 기간
**중위 거래가**(r.amt)였다. 값이 아니라 라벨이 틀렸던 것 — 평당가·전세가율·갭이 전부
이 중위값 기준이라 값 쪽을 최근가로 바꾸면 기준이 뒤틀린다. → 열 이름을 **매매**로
바로잡고, 칸에 중위(큰 글씨) + 최근 실거래 한 건(작은 글씨 · 날짜)을 병기.
읽는 법 주석에 기준을 명시. 나머지 열은 검증 결과 라벨·값 일치.

**③ 거래량 12개월 평균 공백** — 조회 창(24개월) 앞머리 11개월은 창 안에서 평균을 만들
수 없어 —였다(수원 팔달구 2025.07 이전). → `volStoreAt()`: market_core 월 거래량으로
창 밖 과거를 이어붙여 전 구간 계산. 이어붙인 달은 `*` + title로 출처 표시,
저장본 없으면 예전처럼 —.

**④** 가격대별 대출·비용: 15억 <small>이하</small> / 15억 초과(16억 기준) /
25억 <small>이하</small> / 25억 초과(26억 기준) 표기.

### index.html v5.28 — 사용 피드백 8건 (신축 채점 보정 · 10·15 대출 기준)

**① 신축 채점 보정** — "신축·84·8억"에서 시흥 신축이 화성 에듀파크를 이기는 문제.
원인: 이력 2~3년짜리 신축의 자기 CAGR는 분양가·프리미엄 소음인데 그걸로 순위를 매겼고,
거래 꾸준함(sLiq)은 17년 총량이라 신축이 무조건 불리했다. →
- 이력 5년 미만(`cg.yrs < 5`)이면 sPerf = **지역 10년 성과(60%) + 후보들 사이 상대가격·
  대장성(40%)** (`wizRegionCagr` + 2차 대장성 패스). 신축은 입지와 시장이 매긴 가격
  위치가 더 믿을 만하다는 통상 원칙. 카드 칩·왜 추천했나요에 "지역 성과 기준" 명시.
- sLiq = 연평균 거래건수 기준(연 7건 만점) — 신축·구축 공평.

**② 더 보기 누락(호반베르디움더프라임)** — `sel.value = code` 대입이 꼬리 붙은 코드에서
조용히 실패해 이전 지역 표가 그대로 남을 수 있었다 → 옵션을 훑어 매칭(matched 플래그),
기간도 카드 시세표와 같은 6개월로 맞추고, 넘어온 단지 행을 `DJ.flashApt`로 강조.

**③ 10년 추이 상위구** — 전국 서열로 고르니 시흥 위가 대구였다 → `wizUpperOf`:
**같은 도(코드 앞 2자리) 안에서** 위를 찾고, 없으면 최상위 급지 갈래.
자동으로 3칸이 채워지는 이유를 `#cx-autonote` 콜아웃으로 안내(단지명·갈래 설명).

**④ 연식×급 세로 블록** — 안긴 표가 깨진다는 피드백 → 표를 버리고 연식별
`.atgrp` 블록(대장/중위/하위 3줄, 단지 1~2곳이면 중복 제거) — 모바일 친화.

**⑤ 전국 등급표 도움말** — 머리글 클릭 → `#tr-help`에 열 설명, 재클릭으로 닫힘 (15개 열).

**⑥ 거래량 탭 법정동코드 직접 입력 제거** (다른 탭엔 없음 확인).

**⑦ 조건 근거·계산기 = 최근 실거래가** — 시세표가 도착하면 `t.lastDeal`을 저장,
왜 추천했나요 ④와 [이 가격 필요현금]이 최근 실거래(금액·년월·층)를 쓴다.

**⑧ 대출 기준 최신화 (10·15 대책 확인)** — 스트레스 DSR 가산을
**수도권·규제 3.0%p / 그 외 1.5%p**로 분리(stress/stressLocal), finance()가 지역에 따라
적용. `ruleVer: 20251015`로 저장된 옛 기준(1.5)을 자동 이전. 총액상한 6/4/2억·규제지역
목록(2026.7)·LTV는 이미 반영돼 있었음. 전세대출 이자 DSR 산입 등은 미모델링 — 설정 탭에 명시.

### index.html v5.27 — 비교 상대 자동 채우기 · 행렬 표기·정렬 보완

**① [이 단지 10년 추이] 비교 상대까지 자동으로** — 혼자 넣으면 비교가 안 된다는 피드백.
`wizGoCmpx` → `storeTops`(평형대 최근 연 중위가 순 단지 목록) + `wizCmpPlan`(3칸 계획):
- 우리 구 1등이 아니면: 나 + 우리 구 대장 + 한 단계 상위구(RANKED 바로 위) 대장
- 우리 구 1등이면: 나 + 상위구 대장 + 상위구 중위
- 최상위 구(상위구 없음/저장본 없음): 내가 1~3등이면 그 1·2·3등, 아니면 나 + 그 구 1·2등
`cmpSetSlot`이 지역·평형·단지 select를 차례로 채우고 change를 쏜다.
`loadCmpSlot`은 이제 프라미스를 돌려준다(자동 채우기가 순서를 기다려야 해서).

**② 연식×급 행렬 읽기 쉽게** — 칸을 3줄로: 단지명 / 매매 X억 (YY년 중위) / 필요현금 Y억 (방식).
"대출 필" 같은 줄임 제거. 캡션에 매매 = 그 해 거래들의 중앙값(마지막 한 건 아님),
초록 = **시작 탭의 동원 가능 현금** 기준임을 명시(현금 미입력이면 안내).
참고: 추천받기 마법사의 예산은 매매가 필터일 뿐, 채점표의 초록과는 다른 값이다.

**③ [펼치기] 정렬** — cardify가 `tb.querySelectorAll("tbody tr")`로 **안긴 표(.pxt)의 행까지**
집어 라벨을 붙이고 있었다 → 바로 아래 행만 처리(parentNode.parentNode === tb).
카드 모드 CSS도 `table.carded .pxt ...`를 표 형태로 되돌리는 예외 추가.
행렬은 table-layout:fixed(첫 열 104px) + 가로 스크롤.

**④ 가격대별 대출·비용 15억 중복** — 문턱+1만원이 문턱과 같은 "15억"으로 표시됐다 →
문턱 다음 줄은 +1억(16억·26억), 문턱 줄에는 "문턱" 꼬리표. RULES 변경 대비 중복 제거·정렬.

### index.html v5.26 — 사용 피드백 5건 (역세권 보완 · 연식×급 행렬)

**①** 평형 칩 보조 문구("101㎡ 이상") 줄바꿈 방지 — `.sub`에 nowrap, grid4에서 축소.

**②** 왜 추천했나요: 링크 버튼을 내용 아래 한 줄(`.whybtns`)로 모으고 화살표(→) 제거.

**③ 역세권 보완** — 수원 팔달구 검증으로 두 구멍 확인:
- 매교역푸르지오SK뷰: K-apt에 도보 5분만 있고 역명·호선이 없어 v5.22 규칙이 버렸다.
  → `stationFromName()`: **단지명 속 역 이름**("매교역")과 도보 시간(신고값) 두 신호가
  겹칠 때만 역명으로 인정 (~구역·지역 등 오탐 차단). title에 "단지명에서 추정" 명시.
- 힐스테이트푸르지오수원: 호선만 있어 도보 10분*는 떴지만 역세권 태그가 없었다.
  → 호선만 있으면 **역세권\*** 태그(별표 유지). 숫자만 있는 값은 여전히 버린다(봉담 원칙).

**④·⑤ 연식 × 급 행렬** — 지역 중위 하나로는 참고가 안 된다는 피드백.
`ageTierMatrix()`가 trades 저장본에서 조건 평형대의 최근 연 중위가를 단지별로 뽑아
신축(~10년)/준신축(10~20)/구축(20~)으로 가른 뒤 **대장(1위)·중위(중앙)·하위(25% 지점)
단지를 이름째** 보여준다. 필요현금은 단지 자체 전세 실거래로 갭 계산(`cellNeed`),
전세 없으면 대출 기준. 연식 미상·거래 끊긴 단지는 뺀다.
- 지역 채점표 상위 5 카드: 자동 표시(`recoLoadTiers`, loadStore 캐시).
- 전체 채점표: 행마다 **[펼치기]** 버튼 → 그 지역 저장본을 그때 열어 행렬 삽입(detrow).
- 하위 25% 인덱스는 `Math.round((n-1)*0.75)` — floor면 2곳일 때 대장을 가리킨다.

### index.html v5.25 — 마법사 사용 피드백 6건 + 필요현금·갭 기준 교정

**① 평형 조건** — 소형(46↓)·59·84·대형(101↑, 101+135 두 밴드) 4개를 한 줄(`.pickchips.grid4`)로.

**② 결과 카드 시세표** — 추천 계산은 여전히 저장본만 쓰되, 결과가 나온 뒤 그 지역들(≤5곳,
지역당 12회, TRADE_MEMO 캐시 재사용)만 최근 6개월 실거래를 받아 카드마다
매매·전세 × 최근 거래(금액·층·년월) / 6개월 중위 / 최고가(층) 표(`.pxt`)를 채운다.
도착 전에는 저장본 연 중위로 그려 둔다(wizPxStoreHtml → wizPxLiveHtml).

**③ 왜 추천했나요** — 좁은 note 대신 카드 전체 폭의 `.whybox`(근거 4행 grid).
검증 링크는 화면만 열지 않고 **이 단지·지역을 조건에 넣고 조회까지 한다**(`wizGo`):
비교(10년 모드 + 지역·평형·단지 자동 선택), 채점표·등급표(해당 행 강조 `flashRegionRow`),
거래량(지역 넣고 조회), 계산기(매수가·규제·수도권 주입).

**④ 더 보기 조회조건 동기화** — `sel.value`만 넣으면 v5.24의 지역 선택 버튼 라벨이 안 바뀐다.
`fireChange()`로 change까지 쏜다 (채점표 [단지 보기], 갭 표 [단지]도 동일 수정).

**⑤ 필요현금·갭 기준 교정 (중요)** — 안양 만안구 "갭 2.57억"의 원인:
㎡당 중위(전 평형·전 단지) × 84㎡ = 7.5억으로 **매매가를 부풀린 뒤** 84㎡ 전세가율을 곱했다.
실제 84㎡ 중위는 6.4억. 이제 금액 계산(채점표·급지표·갭·지도)은
`regionSale()` = **기준 면적(a84/a59)의 최근 3개 유효월 중위 실거래**(자료 없으면 ㎡당가 환산 폴백),
전세는 같은 면적대 전세가율(`regionJr`, 3개월 중위)로 만든다. financeJeonse가 기준가(sale·je)를
결과에 남겨 채점표 카드에 "중위 매매 X · 전세 Y"로 근거를 보여준다.
주석도 전부 "지역의 중간 단지 기준 — 대장 단지는 30~50% 비쌈"으로 통일.

**⑥ 카드 리디자인** — `.wcard`(그라데이션 배경 + 그림자 + BEST 리본 + 원형 순위 배지 +
큰 점수 + 시세표). 채점표의 `.reco` 카드는 그대로 둔다.

### index.html v5.24 — 지역 선택 UI 통일 (아파트 찾기 판을 전 화면에)

아파트 찾기 탭의 '지역 선택' 판(검색 + 시·도 아코디언 + 격자 + 금색 강조)이 제일 낫다는
피드백에 따라, **지역 1등 아파트**와 **추천 마법사 1단계**도 같은 판을 쓰게 했다.

- `pkEnsureOverlay()` — 판(#pkov) 생성을 단일판(openRegionPicker)과 공용으로 분리.
- `openRegionPickerMulti({isOn, toggle, onChange})` — **여러 곳 고르기 판**.
  같은 겉모습이지만 항목을 눌러도 닫히지 않고 금색 표시만 켜고 끈다.
  그룹마다 [전체 선택/해제] 버튼, 선택 수를 `N / 전체`로 헤더에 표시, 내 지역 ★ 강조,
  검색(pkFilter)·닫기·바깥 클릭 닫기는 단일판과 공유.
  대장 아파트는 **이름**("서울 강남구", subFilter 유지), 마법사는 **코드**를 보관하므로
  isOn/toggle 콜백으로 저장 방식을 각자 정한다.
  `#pklist`는 계속 남는 요소라 위임 처리기는 `onclick` 교체식 — addEventListener를 쓰면 열 때마다 쌓인다.
  단일판을 열 때는 `list.onclick = null`로 다중판 처리기를 걷어낸다.
- 지역 1등 아파트: 안쪽 아코디언(`#fg-picker`, .pgroup/.pitem/체크박스) 제거 →
  [지역 선택하기] 버튼 + 고른 지역 칩(누르면 빼기). `renderWizGu`처럼 `renderFlagShell`도 얇아졌다.
- 마법사 1단계: `#wiz-sido` 셀렉트 + `#wiz-gu` 칩 제거 → 같은 버튼 + 판. `renderWizGu` 삭제(의도된 제거).
- 버튼 라벨은 "고른 지역 N곳"으로 선택 수를 보여준다 (renderPicker/renderWizPicks가 갱신).

### collect.html v2.12 — 새 market_core를 직전본과 대 보기 + 갱신절차.md

매달 갱신에서 가장 흔한 사고는 **CSV를 한두 개 빠뜨리고 올리는 것**이다.
집계는 멀쩡히 끝나고 숫자도 그럴듯해서, 올린 뒤에야 특정 지역이 비었다는 걸 안다.
저장 전에 배포본과 맞대 보는 `[직전본과 비교]`를 넣었다.

`mcDiff(now, old)`가 잡는 것:
  - `m0`가 늦어졌거나 `months`가 줄었는가 → **CSV 누락**
  - 사라진 지역 / 새 지역
  - `stat.saleRows` · `rentRows`가 줄었는가
  - 지역별 **마지막 공통 월**의 중위 평당가가 5% 넘게 튀는가
  - 그 달 거래가 0이 된 지역 — `empties`가 전체의 20%를 넘으면 **그 달 CSV가 통째로 빠진 것**

지역 시계열은 앞을 잘라 두므로(`s.o`) `mcSeriesAt`·`mcNAt`이 offset을 밀어서 읽는다.
결과는 로그에 찍고 `market_core_비교리포트.json`으로도 받는다.

**`갱신절차.md`** — 무엇을 얼마나 자주(market_core 월 1회 · supply 분기 · trades 연 1회 ·
kapt 연 1회), 각 단계, 확인 항목, 자주 나는 사고 표까지 한 문서에 담았다.
이 세션에서 겪은 것들(엑셀 104만 행, 보충본 이중 집계, 502, 일일 한도, 죽은 상위 코드)이 근거다.

### index.html v5.18 — 좁은 화면에서 표를 카드로

휴대폰에서 12칸짜리 표를 가로로 밀어 보는 건 읽기가 어렵다. CSS만으로는 칸 이름을 못 가져오므로
칸마다 머리글을 `data-l`로 붙여 두고, `@media (max-width:720px)`에서 세로 카드로 접는다.

  - `cardify(tb)` — `thead th` 글자를 각 `td`의 `data-l`로 복사. `colspan` 칸은 건너뛰고,
    값이 없는 칸(`—`·`·`)은 `data-none`으로 접는다(12칸이 다 보이면 카드가 화면을 넘긴다).
  - `cardifyAll()` + `watchTables()` — 렌더 함수가 열댓 군데라 하나하나 부르지 않고
    `MutationObserver`로 DOM 변화를 지켜보다 60ms 묶음으로 처리한다.
    `data-l`·class만 바꾸므로(자식 노드는 그대로) 관찰자가 되돌아 돌지 않는다.
  - **다시 그리면 다시 붙인다** — 정렬만 바꿔도 `tbody`가 통째로 갈려 칸이 새로 만들어진다.
    행 수만으로 판별하면 새 칸에 라벨이 안 붙으므로 `첫 칸에 data-l이 있는가`까지 본다.
  - **정렬 칩** — 카드 모드에서는 머리글이 사라져 정렬을 못 누른다. 표 위에 `.sortchips`를 얹고
    지금 기준·방향(↑↓)을 칠한다(`paintSortChips`). 넓은 화면에서는 숨긴다.
  - `td:has(button)`은 한 줄 통째로(담기 버튼), `tr.hit`은 테두리를 금색으로.
    `:has()`를 모르는 브라우저에서는 그 규칙만 무시되고 나머지는 그대로 동작한다.

`.tblwrap` 안의 모든 표에 적용되므로 단지 솔팅·지역 1등·과거 검증·전국 등급표까지 함께 접힌다.

### index.html v5.17 — 비교 탭 경량화 + 10년 모드

순서가 거꾸로였다. 지역을 고르면 **단지를 고르기도 전에** 그 지역 24개월치를 매매+전세로
통째로 받았다(칸당 48회, 세 칸이면 144회). 그 대부분이 드롭다운 하나 채우려고 쓰였다.

  1. **단지 목록은 저장본에서** — `cxStoreApts(st, band)`. 조회 0회, 즉시.
     지역을 바꿔가며 훑어도 공짜다. 최근 24개월에 거래가 없던 단지도 나오므로 고를 폭도 넓다
     (오래 끊긴 단지는 `~2017`처럼 마지막 거래 연도를 붙인다).
  2. **실거래는 단지를 고른 뒤에** — `ensureCmpData(i)`. 고르지 않은 칸은 한 번도 조회하지 않는다.
  3. **기간 세그먼트** — 최근 12개월 · 최근 24개월 · **10년(연 단위)**.
     10년은 저장본만 쓰므로 **조회 0회**이고, 매매·전세·갭·배율이 전부 10년치로 나온다.
     이 탭이 24개월밖에 못 보던 한계가 풀렸다 — 갈아타기 판단엔 긴 흐름이 더 쓸모 있다.
     상승률 창은 월 6칸 / 연 2칸으로 자동 전환(`win`).

**곁들여 고친 것** — `monthlyMedian`이 단지명·법정동을 **글자 그대로** 비교하고 있었다.
저장본은 "상리", 실거래는 "봉담읍 상리"라 읍·면 지역이 통째로 어긋난다. `btKey`로 맞추고,
거래마다 `dealKey(t)`로 한 번만 계산해 붙여 둔다(24개월 × 수백 건이라 반복 계산이 비싸다).

**평당가 표기** — 실거래가 있으면 실제 면적, 저장본만이면 평형대 대표 면적(`BAND_AREA`)으로
어림잡고 `*`를 붙인다. 매매 중위·상승률·갭은 언제나 실제 값이다.

검증(test_cmpx.js 41개): 목록 채우기 실거래 0회, 한 칸만 골랐을 때 0회, 두 칸 고르면 96회,
10년 모드 0회 + 연도 라벨 10칸, 읍·면 법정동 교차 매칭, 저장본 없는 지역은 예전 48회 경로.

### index.html v5.16 — 과거 검증을 저장본으로 (실거래 30회 → 12회)

과거 검증 한 번에 실거래를 **30회** 불렀다.
```
과거 3개월 × 2지역                    =  6회
최근 6개월 × (매매+전세) × 2지역       = 24회   ← 전세 12회는 쓰지도 않는다
```

**(1) 최근 구간은 매매만** — `fetchMonths(lawd, n, onTick, guName, saleOnly)`.
전세 12회가 통째로 버려지고 있었다. 캐시 키를 `:s`로 갈라 전세 없는 결과가 다른 호출자에게 새지 않게 했다.

**(2) 과거 구간은 `trades/` 저장본에서** — `btStoreGroups(st, year, band, mode)`. 조회 0회.
덤이 더 크다: 3개월치가 아니라 **그 해 1년치**가 표본이라 살아남는 단지가 크게 는다
(예전엔 "3개월에 2건 미만" 필터에 걸려 사라지던 단지가 연 단위로는 남는다).
  - 저장본엔 면적이 없다 → 상승률은 **거래가끼리** 비교하고,
    표의 과거 평당가는 `현재 평당가 ÷ (1+상승률)`로 되돌려 추정한다.
    같은 단지·같은 평형대라 면적이 거의 같으므로 **상승률 자체는 정확**하다.
  - 평형 모드는 저장본 밴드를 `bandClass()`로 소형/중형/대형에 묶고 **건수 가중 평균**을 쓴다.
  - `btKey(dong, apt, cls)` — 과거·현재 짝짓기 열쇠. 읍·면은 실거래가 "봉담읍 상리",
    저장본이 "상리"라 `dongKey()`로 맞추고 이름은 `normName()`으로 정규화한다.
  - 두 지역 중 하나라도 저장본이 없으면 **예전 경로(실거래 3개월)로 그대로 떨어진다.**
  - 결과 아래에 과거 구간을 어디서 읽었는지 한 줄로 밝힌다 — 표본 크기가 달라 해석에 영향이 있다.

검증(test_btest.js 45개): 저장본 경로에서 실거래 12회·전세 0회·과거 달 0회,
읍·면 법정동이 어긋난 지역도 짝이 지어짐, 상승률 100% 케이스 검산, 저장본 없을 때 18회로 폴백.

### index.html v5.15 — 근거 없는 '역세권'을 걷어냄

배포된 `kapt/41593.json` 원본:
```json
{"n":"봉담 중흥S-클래스 에듀파크","hh":1050,"pk":1367,"sw":10,"ht":"지역난방","hl":"계단식"}
```
`sw:10`(도보 10분)만 있고 **호선도 역명도 없다.** 봉담읍엔 지하철이 없다.
K-apt의 `kaptdWtimesub`은 **관리사무소가 직접 적어 넣는 칸**이라 이런 값이 흔하다 —
이 파일 한 곳에서만 `sw`는 있는데 `ss`가 없는 단지가 **54곳**이다.

  - `subwayMin()`이 **호선도 역명도 없으면 null**을 돌려준다. 표에도 필터에도 안 잡힌다.
  - **역세권 태그는 역 이름이 있을 때만.** 10분이라는 숫자만으로 역세권을 주장하지 않는다.
  - 호선만 있는 값(에듀시티: 도보 20분 · 1호선·분당선)은 `도보 20분*`처럼 **별표**를 달아 구분.
  - 칸의 `title`에 `3호선 대치 기준 (K-apt 관리사무소 신고값)`을 달아 출처를 밝힌다.

### index.html v5.14 — 화성 봉담읍에서 세대수가 엉뚱하게 붙던 문제

중흥에스클래스 네 단지가 전부 **808세대**로 나왔다. 실제로는 824 · 808 · 1050 · 784다.
원인이 셋 겹쳤고, 배포된 `kapt/41593.json`을 읽어 하나씩 확인했다.

**(1) 읍·면의 법정동 표기가 어긋난다**
```
실거래  "봉담읍 상리"        K-apt  "경기도 화성효행구 봉담읍 상리"  → kaptDong "상리"
```
두 글자가 달라 같은 동으로 못 묶였고, 1:1 보호가 없는 느슨한 전역 탐색으로 새 버렸다.
`dongKey()`로 양쪽 다 **마지막 토막**만 쓴다. `dongStem`도 이걸 먼저 통과시킨다
(안 그러면 "봉담읍 상리" → "봉담읍 상"이 된다).

**(2) 브랜드 표기가 한글/영문으로 갈린다** — `에스클래스` → `s클래스` 별칭 추가.
이걸로 `중흥에스클래스에듀파크` ⊂ `봉담 중흥S-클래스 에듀파크`가 되어 포함 관계로 붙는다.

**(3) 형제 단지에 얹히고 있었다** — 두 겹으로 막았다.
  - `KAPT_SHARE = 0.9` — 공유 배정은 **원점수 0.9 이상**(사실상 포함 관계)일 때만.
    `현대5차 ⊂ 압구정현대`는 통과, `에듀파크` ↔ `에듀시티`는 차단.
  - `tailConflict()` — 앞이 길게 같고 **뒤만 다른** 이름은 형제 단지로 본다.
    공통 접두(3글자 이상)를 뗀 꼬리가 양쪽 다 2글자 이상이면서 겹침이 절반 미만이면 상한 0.4.
    `중흥S클래스에듀파크`↔`에듀포레`, `래미안대치하이스턴`↔`래미안대치팰리스`,
    `봉담자이프라이드시티`↔`봉담자이라젠느`를 갈라낸다.
    접두가 2글자 이하면 판정하지 않는다 — `한양1`↔`한양제1`까지 갈라내면 곤란하다.

검증: 봉담읍 8곳이 전부 제 단지로 1:1, 세대수 8개가 모두 다름. 압구정 차수 공유는 그대로 유지.

### index.html v5.11~v5.13 — 미매칭 명단을 따라가며 마무리

**v5.11** — 미매칭 목록에 **법정동**을 함께 찍는다(`현대2(청담동)`). 어떤 단지가 왜 안 붙는지는
법정동을 봐야 갈린다. 또 지역 전체를 뒤지는 2차 탐색에서 K-apt 이름의 접두를 **그 단지의 동 기준으로**
떼지 못하던 구멍을 막았다 — 후보 변형을 동별로 만들어 `candVars()`로 캐시한다.

**v5.12** — 미매칭 목록을 `title`(마우스 올리기)로만 보여 준 건 실수였다. **휴대폰엔 마우스가 없다.**
숫자를 누르면 표 위에 펼쳐지는 `#dj-missbox`로 바꿨다(`toggleKaptMiss`, `escHtml`).
홈 화면 추가용 메타(`theme-color`, `apple-mobile-web-app-*`)도 붙여 전체 화면으로 뜬다.

**v5.13** — `stripDongTail()`. 이름 뒤에 괄호 없이 붙은 동 번호를 뗀다.
```
실거래  현대1차101동~106동 (개포동)   →  현대1차   →  K-apt 개포현대1차 ✔
실거래  신성아파트101동               →  신성아파트
```
숫자가 앞에 붙은 '동'만 노리므로 `구산동`·`아이파크삼성동`은 건드리지 않는다.

**남은 미매칭의 정체** — 강남 명단을 실제 K-apt 목록과 대조한 결과, 대부분이 **정상**이다.
  - K-apt 미등록(의무관리대상 아님): 에테르노청담·PH129·상지리츠빌카일룸·마크힐스이스트윙·
    BRUNNEN청담·이니그마빌2·목화·한신오페라하우스·연세리버테라스·미켈란107 … 고급 소규모 단지
  - 차수가 실제로 다름: 개포우성2(K-apt엔 1차만) · 청담3/4차 이편한세상(K-apt엔 2차만) ·
    압구정 한양4~7(K-apt엔 제1·2·3단지만) — 숫자 규칙이 일부러 막는다
  - 도곡월드메르디앙·시티프라디움더강남: 해당 동 K-apt 목록에 아예 없음

### index.html v5.10 — 강남 미매칭 142곳을 실제 자료로 뜯어보고 고침

배포된 `kapt/11680.json`을 직접 읽어 원인을 둘로 갈랐다.

**(1) K-apt 이름에 법정동이 접두로 붙는다** — `대치선경` · `대치미도맨션` · `압구정신현대` · `역삼아이파크`.
실거래는 `선경1차(1동-7동)` · `한보미도맨션1` · `신현대9차` · `역삼I'PARK`.
  - `dongStem("대치동") → "대치"`를 **양쪽에서 떼고도** 대 본다(`nameVariants(name, dong)`).
    선경1 ⊂ 선경, 한보미도맨션1 ⊃ 미도맨션이 되어 붙는다.
  - `NAME_ALIAS` — ipark↔아이파크, e편한세상↔이편한세상, SK VIEW↔SK뷰, 더힐↔the hill.
    작은따옴표(`'`·`’`)도 제거 대상에 추가.

**(2) K-apt와 실거래의 단위가 다르다** — K-apt 압구정동은 관리주체 11곳뿐인데
(`압구정현대아파트` · `압구정 현대(10,13,14차)` · `압구정현대8차` · `압구정신현대` …)
실거래는 `현대1차`~`현대14차`, `신현대9/11/12차`처럼 차수별로 쪼갠다. 1:1만 고집하면 나머지가 통째로 빈칸.
  - `matchKaptAll`이 **1차 1:1 → 2차 공유** 2단계로 배정한다. 남은 단지는 이미 쓰인 K-apt에 붙되
    `shared: true`.
  - 표에서 공유 세대수는 `1,924~`처럼 **~**를 달고 흐리게, `title`에 "관리단지 전체 세대수" 표시.
    세대수는 그 차수 값이 아니므로 값은 주되 무엇인지 밝힌다.
  - 반환형이 `{ it, shared }`로 바뀌었다. `matchKapt()`는 예전처럼 항목만 돌려준다.
  - 문턱 `KAPT_LOOSE` 0.45 → 0.42.

검증: 압구정동 실거래 17곳(현대 1~14차·신현대 3곳·한양1차·미성 2곳·하이츠파크) **미매칭 0곳**,
미성1차·2차는 1:1 유지, 신현대 3곳은 `압구정신현대` 하나를 공유. 세종 고운동 14곳은 여전히 전부 1:1.

### index.html v5.9 · collect.html v2.10~v2.11 — 짝짓기 정확도와 수집기 복구력

**수집기가 죽은 코드를 돌던 문제 (v2.11)** — collect.html의 LAWD 표는 **국토부 CSV 내려받기용**이라
상위 시 코드(41110 수원시)와 옛 코드(42110 춘천시)가 섞여 있다. K-apt에는 그런 코드가 없어
`{"error":"K-apt 단지목록 조회 실패","tried":[…"err":"empty"]}` → 502로 돌아온다.
경기 55곳 중 8곳, 강원 36곳 중 18곳이 그랬다. 이제 `kgCodes()`가 **market_core.json의 지역 코드**를
정본으로 삼는다 — 앱이 실제로 `kapt/{lawd}.json`을 찾는 253곳과 정확히 같다.

**수집기 복구력 (v2.10)** — 502·504를 1s→2.5s→5s→10s로 재시도, 지역 하나 실패는 건너뛰고 계속,
실패 지역은 **저장하지 않아** 다음 회차에 다시 받는다. 응답 본문 앞 160자를 로그에 실어 원인이 보인다.
단지는 있는데 정보가 하나도 안 붙은 지역도 저장을 거부한다 — 빈 파일이 굳으면 영영 —로 남기 때문.
`[점검]` 버튼이 지역별 채움률을 보고한다.

**짝짓기 (v5.9)** — 실제 자료를 보고 고쳤다. 세종 고운동:
```
K-apt   "세종5차 호반베르디움 가락마을20단지아파트"
실거래  "가락마을 20단지(호반베르디움5차)"
```
  - `nameVariants()` — 괄호 안팎을 각각 변형으로 만들어 어느 쪽이든 맞으면 성공
  - `numConflict()`를 **자리별 비교 → 포함 관계**로. 위 예는 숫자가 [5,20] vs [20,5]로 순서가
    뒤집혀 자리별로는 탈락한다. 이제 `{20} ⊂ {5,20}`이면 통과, `{1}` vs `{4}`나 `{5}` vs `{15}`는 탈락
  - `kaptDong()` — 주소 마지막 토큰과 법정동을 **정확히** 비교 (예전 `indexOf`는 '의동'이 '구의동'에 걸렸다)
  - `matchKaptAll()` — 지역 전체를 **1:1 그리디 배정**. 점수 높은 짝부터 확정해 한 K-apt 단지가
    두 곳에 붙지 않는다. 같은 동 안이면 문턱 0.45(+동 보너스 0.15), 동을 못 찾았을 때만 전 지역 0.68
  - `normName`에 차수 통일(제3차·3차 → 3)과 기호 제거 확대
  - 못 찾은 단지 이름을 조회 정보 `title`에 달아 마우스로 확인 가능
  - `attachHouseholds`와 후보 담기도 저장본을 먼저 보고, 짝짓기를 한 번만 한다

**남은 한계** — K-apt는 의무관리대상 단지만 담고, 등록된 단지도 세대수 칸이 빈 곳이 있다
(세종 216곳 중 hh 207 · pk 215 · sw 39). 지하철이 없는 지역은 sw가 낮은 게 정상이다.

### index.html v5.7~v5.8 · collect.html v2.9 — 단지정보를 파일로, 전세 빈칸을 저장본으로

**평당가 표기** — `pyFmt()`. 1억 이상은 `2.28억`(소수 둘째 자리 고정), 미만은 `1,964만`.
`eok()`처럼 뒤 0을 지우면 20,008과 22,757이 둘 다 "2억"이 되어 서열이 사라지므로 자릿수를 고정했다.

**전세 폴백** — 조회 기간(최근 3~12개월)에 같은 평형대 전세 계약이 없는 단지가 흔하다.
`storeJeonse(st, cx, band)`가 `trades/` 저장본에서 가장 최근 연 중위가를 끌어와
`3.2억 (24년)`처럼 **연도를 붙이고 흐리게**(opacity .62) 보여준다. 전세가율·갭도 같이 흐려지고,
옛 값일 때는 전세가율 70% 이상 강조를 하지 않는다 — 지금 시세가 아니기 때문이다.

**K-apt 확대** — 상위 40곳 제한을 없애고 표에 나온 전 단지를 채운다.
  - 호출 풀 분리: 실거래 `API_CONC=3`, 단지정보 `KAPT_CONC=6` (`apiVia`로 공통화)
  - `localStorage` 영구 캐시 `trackapt_kapt_v1` — 12개 필드만 남겨 저장, 용량 초과 시 절반 폐기 후 재시도
  - 세대수 0은 **결측**으로 처리 (K-apt가 0을 주는 단지가 있다)
  - 이름 매칭 개선: `numsOf()`로 숫자열이 다르면 점수 상한 0.4 — 개포주공1단지 ≠ 개포주공4단지.
    같은 법정동 안에서 0.5 이상이면 채택 — 향촌롯데 ↔ 향촌마을롯데를 잡는다.

**단지정보 저장본 `kapt/{lawd}.json` (v1)** — collect.html v2.9의 새 카드가 만든다.
```
{v:1, code, nm, n, cx:[{k:단지코드, n:이름, a:주소, hh:세대수, dc:동수, b:건설사,
                        pk:주차, sw:지하철 도보 분, sl:호선, ss:역, ht:난방, hl:복도}]}
```
  - 시·도를 골라 나눠 받는다. 받은 지역은 **IndexedDB**(`trackapt_kapt`)에 쌓여 브라우저를 닫아도 남는다
    (localStorage 5MB로는 전국 약 2만 단지가 안 들어간다)
  - 일일 트래픽 한도 응답(`LIMITED_NUMBER…`)을 만나면 **재시도 없이 즉시 전체 중단** — 다음 날 이어서 하면
    이미 받은 지역은 건너뛴다
  - `kgTx`는 결과를 `box.v`에 담고 `oncomplete`에서 꺼낸다 (요청 onsuccess가 항상 먼저라는 IDB 규칙)
  - 앱은 `loadKaptStore()`로 이 파일을 먼저 보고, 있으면 **목록조차 부르지 않는다**(API 0회).
    없으면 예전 경로(목록 1회 + 단지별 상세)로 떨어진다.

### collect.html v2.8 · index.html v5.6 — 저장본을 분기 → 연 단위로 (스키마 v2)

**분기는 단지 단위에 과한 정밀도였다.** 한 단지의 한 분기 거래는 0~2건이라 값이 흔들리고
파일만 커졌다. 실사용에서 **GitHub 웹이 994KB짜리 한 줄 JSON을 못 열어**("No server is
currently available") 파일 확인·삭제가 불가능한 상태가 됐다.

- 연 단위(17칸)로 접었다: **최대 657KB → 226KB · 총 40MB → 14MB.**
  표본은 4배가 되어 값이 오히려 안정적이고, 과거 검증(5·10·15년)·비교 시계열엔 충분하다.
- **단지마다 줄바꿈**을 넣었다(크기 +6%). 한 줄짜리 큰 JSON은 GitHub 뷰어가 시간 초과로 못 연다.
- 스키마 **v2**: `q0/quarters` → `y0/years`, `v:1` → `v:2`.
  앱의 `loadStore`가 **`v !== 2`면 무시**한다 — v1(분기) 파일이 남아 있으면 한 칸이 1년이 아니라
  1분기라 기간이 4배 어긋난 CAGR이 조용히 나온다. 버전 체크가 그걸 막는다.
- `storeCagr`: `last - years * 4` → `last - years`, 보간 폭 ±2분기 → ±1년.

**운영 교훈**
- 저장소에 올릴 파일은 **사람이 GitHub 웹에서 열 수 있는 크기**여야 한다. 안 그러면 확인도
  삭제도 못 한다(웹 UI엔 폴더 삭제가 없고, github.dev의 `.` 단축키로만 가능).
- github.dev에서 파일 옆 `A`는 **Added = 아직 커밋 안 됨**. 소스 제어 패널에서 커밋해야 반영된다.
- `trades/`는 **매달 갱신할 필요가 없다.** 과거 실거래는 거의 안 변하고 최근 구간은 API가 덮으므로
  연 1회면 충분하다. 매달 갱신하면 253개 파일이 전부 바뀌어 히스토리가 급격히 불어난다.
- 검증: `test_store.js` **66항목** · `test_v50.js` **157항목**.

### collect.html v2.7 — 단지별 저장본 trades/{lawd}.json 생성기

3층 구조의 **2층**. 시·군·구마다 `단지 × 평형대 × 분기` 중위가·건수를 담아, 앱이 그 지역을 볼 때만
지연 로드한다. 253개 파일을 **ZIP 하나**로 받는다(브라우저에서 store-only ZIP을 직접 만든다).

- **왜 분기인가**: 단지 하나의 한 달 거래는 0~2건이라 월별 중위가는 노이즈다. 분기로 묶으면
  표본이 3배가 되고 파일도 3배 작아진다. 최근 구간은 앱이 API로 덮으므로 손해가 없다.
- 평형대는 앱의 `BANDS`와 동일 — 36(20~45)·46(45~55)·59(55~70)·84(70~95)·101(95~120)·135(120~).
- 단지 식별 = **단지명 + 법정동**. 건축년도는 최빈값(오타·차수 혼재 대비).
- 스키마: `{ v, code, nm, q0:"2010Q1", quarters, bands, cx:[{n,d,y,s:{밴드:{o,p,c}},r:{...}}] }`
  `p`=분기 중위 거래금액(만원) · `c`=건수 · `r`=전세(월세 0원)만.
- `splitRow(line,K)`: 뒤에서 K개를 떼고 남은 앞부분을 `시군구,번지,단지명`으로 가른다.
  단지명에 쉼표가 있어 따옴표로 감싸도 정확히 복원된다(`""` 이스케이프 포함).
- 보충본 셀 단위 교체(v2.6 규칙)를 그대로 따른다.

- **[함정] `median()`을 그대로 쓰면 안 된다.** market_core의 `median()`은 표본 3건 미만을
  버리는데(시군구 집계라 그게 맞다), 단지 한 분기 거래는 대개 1~2건이라 **대부분이 사라진다.**
  하한 없는 `medianAll()`을 따로 두고, 대신 건수 `c`를 함께 담아 앱이 신뢰도를 판단하게 했다.
  테스트가 "3분기 값 null" 로 잡아냈다.
- 검증: `test_store.js` **63항목** (밴드 경계·분기 인덱스·따옴표 단지명·분기 접기·
  ZIP 로컬헤더/EOCD/CRC32·해제 제외·월세 제외·건축년도 최빈·전체 파이프라인·기존 기능 회귀).

### collect.html v2.6 — 보충본 셀 단위 자동 병합 · JSON BOM 제거

**[발견] v2.5 첫 생성분에 매매 이중 계상.** 매매 파일을 31개(원본 17 + 보충 14) 넣었더니
잘림 셀 347개에 원본의 잘린 1,000행과 보충본의 전량이 **둘 다** 들어갔다.
- 근거: 집계 총계 9,281,875 + 해제 188,793 = **9,470,668** = 원본 9,033,650 + 보충 437,018.
  확정 병합본은 9,123,668이므로 정확히 **347셀 × 1,000행 = 347,000행 초과**.
  표본 확인: 26350-201911이 2,891건(정답 1,891) · 30170-201911 2,374(1,374) · 41117-201912 2,373(1,373).
- 조치: 병합본을 따로 만들 필요 없이 **수집기가 알아서 셀 단위 교체**하도록 했다.
  · `isSupFile(name)` — 파일명에 "보충"이 있으면 보충본.
  · `mc-go`가 잡 목록을 정렬해 **보충본을 항상 먼저** 읽고, 그 (코드×월) 셀을 `acc.repl`에 기록.
  · 이어서 읽는 원본에서 같은 셀의 행은 통째로 버린다(`acc.dropped`). PROJECT의
    "원본에서 잘림 셀 행을 전부 삭제 후 보충본으로 교체" 규칙과 동일.
  · 결과 로그·`stat.replCells`/`stat.dropped`로 교체 내역을 노출한다.
  · 정렬 순서가 이 방식의 전제다 — 원본을 먼저 읽으면 교체가 안 된다(테스트로 명시).
- **[버그] download()가 .json에도 BOM을 붙였다.** `Response.json()`은 BOM을 벗기지만
  `JSON.parse`·파이썬 `json.load`는 깨진다. 확장자가 .json이면 BOM 없이·mime을
  application/json으로 저장하도록 수정.
- 검증: `test_merge.js` **20항목 통과** (보충 판별, JSON/CSV BOM, 원본 1,000+보충 1,891 → 1,891,
  안 잘린 셀 불변, 보충 없을 때 무영향, 순서 역전 시 이중 계상 재현 + 정렬 코드 존재 확인, stat 노출).
  전체 **143항목** (50 + 13 + 60 + 20).

### market_core.json 확정 (2026.08, v2.6) — 검증 통과

v2.6 재생성분이 모든 기준을 통과했다. **이것이 3층 구조 1층의 확정본이다.**

- 기간 201001~202606 · 198개월 · 지역 **253곳** · 묶음 17개(시도 16 + 전국).
  광주·전남이 2026.7 개편으로 "전광"에 흡수돼 시도 라벨이 16개다.
- **매매 9,123,668행 = 집계 8,945,021 + 해제거래 178,647** → 확정 병합본과 **완전 일치**.
  보충 교체 셀 347개 · 원본에서 버린 행 347,000. 잘림 셀 표본이 공식본과 일치:
  26350-201911 **1,891** · 30170-201911 **1,374** · 41117-201912 **1,373**.
- 전월세 13,129,197행 · **월세 빈칸 0행** · 건너뛴 행 9 · 파일 2.11MB.
- 연도별 매매 건수가 시장사와 부합: 2020년 814,481(패닉바잉 최다) · 2022년 258,369(거래절벽 최저) ·
  2026년 상반기 281,503.
- 전세 비중 74.2%(2011) → 61.8%(2021) → **49.3%(2026)**. 2021.6 전월세 신고 의무화 이후
  월세화가 뚜렷하고, 보충분 표본에서 본 추이(월세 21.8%→53.2%)와도 맞는다.
- 거래가 있는데 중위 평당가가 null인 달 1,284 / 49,565 (2.6%) — 전부 표본 3건 미만.
- 표본 점검(강남 11680): 2026.06 84㎡ 중위 30.2억 · 평당 12,166만원 · 전세 84㎡ 9.25억
  (전세가율 30.6%). 2010.01 평당 3,747 → 2026.06 12,166 (×3.25).
- v2.6.1: 결과 로그를 `매매 원본 N행 → 집계 M행 (해제거래 K행 제외)`로 고쳤다.
  기존 문구가 "집계 M행(해제 제외 K)"이라 총계가 안 보여 혼동을 줬다.

### market_core.json 첫 생성 결과 (2026.08, v2.5 — 매매 이중 계상, 폐기)

- 기간 201001~202606 · 198개월 · 지역 **253곳** · 묶음 17개(시도 16 + 전국).
  광주·전남은 2026.7 개편으로 "전광"에 흡수돼 시도 라벨이 16개다.
- 매매 9,281,875행(해제 제외 188,793) ← **이중 계상, v2.6으로 재생성 필요**
- 전월세 13,129,197행 · **월세 빈칸 0행** ✅ (재수집 성공 확인. 옛 병합본 13,128,868과 +329 차이는
  API 측 정정분으로 판단) · 건너뛴 행 9 · 파일 2.11MB.
- 표본 점검(강남 11680): 2026.06 84㎡ 중위 30.2억 · 평당 12,166만원 · 전세 84㎡ 중위 9.25억
  → 전세가율 30.6%. 2010.01 대비 평당 3,747 → 12,166 (×3.25). 값 자체는 정상 범위.
- **재생성 후 합격 기준: `saleRows + saleCancel == 9,123,668`, `replCells == 347`, `dropped == 347,000`.**

## 매매 원자료 확정 (2010.01~2026.06) — 병합 완료

- 프록시 v2(페이징) 교체 후 347 잘림 셀 보충 수집 → **437,018행 수령**.
  347셀 전부 도착·초과 셀 0·여전히 1,000인 셀 0. 핵심 셀 전수 일치
  (해운대 201911 1,891 / 대전서구 201911 1,374 / 영통 201912 1,373 / 일산서 201911 1,170).
- 병합 방식: 원본 연도 파일에서 **잘림 셀의 행을 전부 삭제 후 보충본으로 교체**
  (부분 병합 시 중복이 생기므로 셀 단위 통째 교체). 결과 `/home/claude/merged/매매_YYYY.csv` 17개.
- 규모: 9,033,650 → **9,123,668행 (+90,018)**. 2020년이 +47,768로 최다(139셀 잘림).
  2022~24는 잘림 0(거래절벽).
- 최종 검증(2019.08~12, 공식 대비): 295,645 vs 295,633 = **−12건 (0.004%)**.
  월별 −1~−4로 균일 → 특정 지역·구조적 누락 아님. API 측 정정·삭제 시점 차이로 판단, 허용.
- 동일 행 중복은 원자료 특성: 공식 다운로드본에도 동일키(단지·일자·금액·층) 중복
  2,780건 존재(도시형생활주택 등 같은 날 복수 호실 거래). **중복 제거하면 안 됨.**

## 매매 최종 검증 (2020~2026 공식본 대조) — 확정

병합본 vs 실거래가 공개시스템 다운로드본, 79개월 전수 대조.

- **71개월 오차 −1~−7건**(0.005% 미만). 계약일·아파트 기준이 같으므로 사실상 완전 일치.
- 어긋난 8개월은 **전부 공식 파일 쪽 결함**으로 판명:
  · 2022 파일: 2022.01·02가 통째로 없고 대신 2020.01·02 데이터가 들어있음. 2022.09는 339건
    (전국 한 달이 339건일 수 없음). → 병합본(22,528 / 24,471 / 16,518)이 정답.
  · 2025 파일: 2025.02(13,981)·03(1,551)·10(27,338)이 잘려 있음 → 병합본이 정답.
  · 2020.02: 공식 2020 파일 79,376 vs 병합 83,353. 그런데 **공식 2022 파일에 들어있던
    202002 값이 정확히 83,353** → 병합본이 정확하고 2020 파일이 3,977건 적게 받아진 것.
- 2026.07(40,987건)은 신고기한 미완성 구간이라 저장본에서 의도적으로 제외(2026.06까지).
- **결론: 매매 원자료 2010.01~2026.06 = 9,123,668건으로 확정. 재수집 불필요.**
- 교훈: 공식 다운로드본도 결함이 있을 수 있다. 단일 파일을 절대 기준으로 삼지 말고
  월별 분포·다른 연도 파일과 교차 대조할 것.

## 전월세 진단 (2011.01~2026.06) — collect v2.3

- 수령: ZIP 16개 파일(2011~2026, 1.3GB), **총 12,313,005행**. 매매(912만)보다 35% 많다.
  · **2010년 파일 없음** — 매매는 2010.01부터라 기간이 어긋난다. 재수집 또는 시작점 통일 필요.
  · 월 커버리지 결손 0 (2011.01~2026.06 전월 존재).
- **잘림 셀 2,202개** (전부 정확히 1,000건, 초과 0) — 매매 347개의 6.3배.
  연도별 증가: 2011년 42 → 2019년 85 → 2022년 325 → 2023년 343(최다).
  전월세 신고 의무화(2021.6) 이후 급증. 최다 코드: 강남 183 · 송파 169 · 남양주 169 ·
  노원 163 · 분당 129회.
- 회수 예상 규모: 2,202셀 × 평균 초과분 → **20만~40만 행** 추정.
- collect v2.3: **[전월세 잘림 2,202셀 채우기]** 버튼 추가(`RENT_CAPPED` 상수 내장).
  자료 종류를 rent로 자동 전환(종류 불일치로 엉뚱한 데이터를 받는 사고 방지).
  매매 버튼도 sale로 자동 전환하도록 대칭 수정.
- 검증: test_collect2 41항목 통과.

## 전월세 보충 병합 + [치명] 원본 월세 필드 전량 결손 발견

### 보충 회수 (성공)
- 보충 16개 파일 = **3,017,863행**. 2,202셀 전부 도착·대상 외 0·미도착 0.
- 병합 후: 12,313,005 → **13,128,868행 (+815,863)**. 2011.01~2026.06 · 186개월 결손 0.
- 정확히 1,000인 셀 4개(41150-202112 · 41390-202006 · 41465-202111 · 41465-202509)는
  **일자 분포가 1일~말일 고르게 퍼져 있어 실제 값이 1,000건**으로 판정(재잘림 아님).

### [치명] 원본 전월세의 월세(만원) 열이 전부 빈칸
- 원본 16개 파일 **12,313,005행 전부 월세 = 빈칸**. 보충분(새 프록시)만 정상
  (2023년 보충 507,073행 중 월세 있음 231,294 = 45.6%).
- 원인: **옛 api/trades.js가 전월세 응답에서 monthlyRent를 정규화하지 않았다.**
  잘림(페이징)과는 별개의 두 번째 결함. 새 프록시(api-trades.js)는 deposit·monthlyRent·
  contractTerm·contractType을 모두 매핑하므로 보충분은 정상.
- 영향: 전세/월세 구분 불가 → **전세가율·갭투자 지표를 만들 수 없다.** 보증금만 있고
  월세를 모르면 반전세를 전세로 오인해 전세가율이 과대 계상된다.
- 보충분 월세 비율 추이(21.8% 2011 → 47.8% 2022 → 53.2% 2026)는 시장 통계와 부합 —
  새 프록시 데이터는 신뢰 가능.
- **결론: 전월세는 잘림 셀만이 아니라 전 구간 재수집이 필요하다.**
  약 186개월 × 284코드 ≈ 53,000회(+페이징) · 2~3시간. 매매는 영향 없음(월세 개념 없음).

> ✅ **[해결 완료]** 이 진단대로 전 구간을 재수집했고 결과는 위쪽
> "market_core.json 확정 (2026.08, v2.6)" 절에 있다 — **전월세 13,129,197행 ·
> 월세 빈칸 0행(rentBlank: 0)**. 배포본 market_core.json의 `stat`으로도 확인된다.
> **이 문서는 최신 항목이 위**다. 파일 끝(=가장 오래된 항목)의 "필요하다"를
> 남은 할 일로 읽지 말 것 — 2026.08 세션에서 실제로 그렇게 오독한 적이 있다.
