# TrackApt 자료 갱신 절차

> 이 문서 하나만 보고 갱신할 수 있게 적었습니다.
> 모든 작업은 브라우저에서 합니다 — 개발 환경 없이 **수집기 화면 + GitHub 웹 업로드**로 끝납니다.
> 수집기: <https://track-apt.vercel.app/collect.html>

---

## 무엇을 얼마나 자주

| 파일 | 담는 것 | 주기 | 걸리는 시간 | 안 하면 |
|---|---|---|---|---|
| `market_core.json` | 시군구 × 월 지수·중위가·거래량 | **월 1회** | 20~40분 | 시장 공부·급지표·타이밍이 옛 달에 멈춤 |
| `supply_data.json` | 미분양·착공·준공 | **분기 1회** | 10분 | 입주물량·수급 카드가 옛 분기에 멈춤 |
| `trades/*.json` | 단지 × 평형대 × 연 중위가 | **연 1회** (1월) | 1~2시간 | 10년 열·과거 검증·비교 10년 모드가 작년까지만 |
| `kapt/*.json` | 단지 세대수·지하철·건설사 | **연 1회** | 며칠 (한도 때문) | 새로 입주한 단지가 `—`로 남음 |

**최근 몇 달은 갱신하지 않아도 앱이 실거래 API로 덮습니다.** 갱신이 하루이틀 늦어도 화면이 비지 않습니다.
급한 건 `market_core.json` 하나뿐이고 나머지는 훨씬 느긋해도 됩니다.

---

## 1. market_core.json — 월 1회

### 1-1. CSV 받기

수집기 첫 화면(**수집 조건**)에서

1. `자료 종류` = **아파트 매매**
2. `시작`·`종료` = 갱신할 구간. **처음부터 다시 받지 말고 최근 1~2년만** 받으면 됩니다
   (뒤에서 예전 CSV와 함께 넣습니다)
3. `지역` = 전체 선택
4. **[수집 시작]** → 연도별 CSV가 자동 저장됨
5. `자료 종류`를 **아파트 전월세**로 바꿔 같은 구간을 한 번 더

> `코드 자동 탐지`는 **행정 개편이 있었을 때만** 켜세요. 40~60분 걸리고, 한 번 켜면 결과가
> 브라우저에 남아 다음부터는 안 걸립니다. 2026.7 인천 4구 신설·전남광주통합 때 이게 없어서
> 27개 시군구가 통째로 빠졌던 적이 있습니다.

### 1-2. 집계

**market_core.json 만들기** 카드에서

1. `매매 CSV` = **2010년부터 지금까지 전부** (새로 받은 것 + 예전 것)
2. `전월세 CSV` = **2011년부터 전부**
3. 보충본(`..._보충_2019.csv`)이 있으면 **같이 넣으세요** — 수집기가 파일명을 보고 원본을 덮어씁니다.
   따로 빼거나 엑셀에서 합칠 필요 없습니다
4. **[집계 시작]**

### 1-3. 올리기 전에 반드시 — [직전본과 비교]

**[직전본과 비교]** 를 누르면 배포 중인 `market_core.json`과 맞대어 봅니다.

- 기간이 줄지 않았는지 (줄었으면 **CSV를 빠뜨린 것**)
- 지역이 사라지지 않았는지
- 매매·전월세 행 수가 줄지 않았는지
- 지역별 중위 평당가가 5% 넘게 튀는 곳
- 마지막 달 거래가 0이 된 지역 — 여러 곳이 한꺼번에 비면 **그 달 CSV가 통째로 빠진 것**

> 매달 갱신에서 가장 흔한 사고가 **CSV 한두 개를 빠뜨리는 것**입니다.
> 집계는 멀쩡히 끝나고 숫자도 그럴듯해서, 이 비교가 아니면 올린 뒤에야 압니다.

⚠ 가 하나도 없으면 **[market_core.json 저장]** → GitHub 저장소 루트에 업로드(덮어쓰기).

### 1-4. 확인

앱을 새로고침하고 오른쪽 위 `실거래 2026.06 · 시군구 253곳` 표시가 **새 달**로 바뀌었는지 봅니다.

---

## 2. supply_data.json — 분기 1회

미분양·착공·준공 통계는 국가통계포털(KOSIS)에서 받아 합칩니다.
받은 CSV를 저에게 주시면 병합해 드립니다 — 이 파일은 수집기가 만들지 않습니다.

바뀐 뒤 확인: **어디를 살까 → 입주물량** 탭의 착공·준공 차트 오른쪽 끝이 새 분기까지 오는지.

---

## 3. trades/ — 연 1회 (1월)

과거 실거래는 거의 바뀌지 않고 최근 몇 달은 API가 덮으므로 **일 년에 한 번이면 충분합니다.**
자주 올리면 git 이력만 무거워집니다.

1. 수집기 **단지별 저장본 만들기** 카드
2. `매매 CSV` 전부 + `전월세 CSV` 전부 (market_core와 같은 파일들)
3. **[단지별 저장본 만들기]** → 결과 로그에 `지역 253개 · 단지 4만여 곳 · 2010년부터 N개 연도`
4. **[trades.zip 저장]** → 압축 풀어 저장소의 `trades/` 폴더에 업로드

> 253개 파일이라 한 번에 안 올라가면 **80~90개씩 나눠서** 올리세요.
> 앱은 `v`가 2가 아닌 파일은 무시하므로, 옛 파일이 섞여도 잘못된 값이 나오지는 않습니다.

확인: GitHub에서 `trades/11680.json`을 열어 내용이 보이는지 (유니콘 오류가 안 나는지).
앱에서 아파트 찾기 → 강남구 → **10년** 열이 채워지는지.

---

## 4. kapt/ — 연 1회

세대수·지하철·건설사는 K-apt에서 받습니다. **공공데이터 일일 트래픽 한도** 때문에
전국을 한 번에 못 받는 게 정상입니다.

1. 수집기 **단지정보 저장본 만들기** 카드
2. 시·도를 **몇 개씩 골라** [단지정보 모으기]
3. 한도에 걸리면 그 자리에서 멈춥니다 → **다음 날 다시 누르면 남은 지역부터** 이어서 합니다
   (받은 지역은 브라우저에 남습니다)
4. 중간중간 **[점검]** 으로 채움률 확인 — 50% 미만인 지역이 있으면 다시 받는 편이 낫습니다
5. **[kapt.zip 저장]** → 압축 풀어 `kapt/` 폴더에 업로드

> **채움률 100%는 안 나옵니다.** K-apt는 의무관리대상 단지(300세대 이상 등)만 담고 있어
> 소규모 단지는 애초에 없고, 등록된 단지도 세대수 칸이 빈 곳이 있습니다.
> 세종 기준 세대수 96% · 주차 99% · 지하철 18%(지하철이 없는 지역이라 정상)였습니다.

---

## 자주 나는 사고

| 증상 | 원인 | 대처 |
|---|---|---|
| 특정 지역만 값이 텅 빔 | 그 지역 CSV를 빠뜨림 | [직전본과 비교]로 확인 후 다시 집계 |
| 행 수가 예상보다 많음 | 보충본을 원본과 **둘 다** 넣고 합쳐짐 | 수집기 v2.6+ 는 자동 처리 — 로그의 `보충 교체로 버린 N행` 확인 |
| 엑셀이 "완전하게 로드하지 못했습니다" | 한 해가 104만 행을 넘음 | 파일은 멀쩡함. 수집기가 `_1of2.csv`로 나눠 저장 |
| 수집기에서 502 | 국토부 쪽 일시 먹통 | 자동 재시도(4회). 지역 하나만 건너뛰고 계속하니 끝나고 다시 누르면 됨 |
| `LIMITED_NUMBER...EXCEEDS` | 공공데이터 일일 한도 | 다음 날 이어서 (한도는 **서비스별** — 목록은 되는데 기본정보만 막힐 수 있음) |
| `auth:20` / `SERVICE_ACCESS_DENIED` | **활용신청 만료·미승인** — 기다려도 안 풀림 | data.go.kr 마이페이지 → 활용신청 목록에서 '공동주택 기본/상세 정보제공 서비스' 상태 확인 → 연장·재신청 (승인 후 반영 몇 시간) |
| 지역 목록에 없는 코드로 502 | 옛/상위 코드 (41110 수원시 등) | 수집기 v2.11+ 는 `market_core.json` 기준 253곳만 돎 |

---

## ★ 어느 달까지 받을지 — 신고기한 규칙

실거래 신고기한은 **계약일로부터 30일**입니다. 그래서 **직전 달은 항상 미완성**이고,
그 앞 달까지만 넣어야 합니다. 지난달을 넣으면 거래량이 뚝 떨어진 가짜 절벽이 생깁니다.

| 지금이 | 안전하게 넣을 수 있는 마지막 달 | 넣으면 안 되는 달 |
|---|---|---|
| 9월 초 | **7월** | 8월(9월 말까지 신고 가능) |
| 10월 초 | 8월 | 9월 |

**받는 구간은 "올해 1월 ~ 마지막 달"로 잡으세요.** 수집기는 연도별로 한 파일을 만들기 때문에
(`아파트_매매_실거래가_2026.csv`), 올해분을 통째로 다시 받으면 **먼저 넣었던 달의 뒤늦은
신고분까지 자동으로 메워집니다.** 새 달만 따로 받으면 앞 달의 누락이 그대로 남습니다.

> 예) 8월에 6월까지 넣었다면, 그때 6월은 아직 신고 중이라 덜 찼습니다.
> 9월에 **2026-01 ~ 2026-07**로 다시 받으면 6월 누락분이 채워지고 7월이 새로 붙습니다.

---

## 순서 요약 (매달)

```
1. 수집기 → 최근 1~2년 매매 CSV 받기
2. 수집기 → 같은 구간 전월세 CSV 받기
3. market_core 카드에 전 연도 CSV 넣고 [집계 시작]
4. ★ [직전본과 비교] — ⚠ 없으면 통과
5. [market_core.json 저장] → GitHub 루트에 덮어쓰기
6. 앱 새로고침해서 상단 날짜 확인
```

1월에는 여기에 **3번(trades/)** 을, 필요하면 **4번(kapt/)** 을 더합니다.
